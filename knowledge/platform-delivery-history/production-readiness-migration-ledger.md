---
schemaVersion: treeseed.knowledge-page/v1
id: production-readiness-migration-ledger
bookId: platform-delivery-history
slug: production-readiness-migration-ledger
title: Production Readiness Migration Ledger
summary: Historical execution ledger retained as immutable migration evidence.
status: published
visibility: team
order: 30
contributors: []
relatedBookIds: []
relatedKnowledgeIds: []
relatedNoteIds: []
relatedQuestionIds: []
relatedObjectiveIds: []
relatedProposalIds: []
relatedDecisionIds: []
guaranteeIds: []
audiences:
  primary:
    - platform-maintainers
  secondary: []
  excluded: []
capabilityIds: []
routePatterns: []
resourceTypes: []
actionIds: []
keywords: []
documentationUrls: []
---

> Archived verbatim from docs/production-readiness-migration-ledger.md at SHA-256 202f00b39aa55b6a11af13bcf8cdaa69aea355504c50bf30fd9549b0178efd7e. This is historical evidence, not current operator guidance.

# Production Readiness Migration Ledger

This document is the durable execution ledger for the Platform, Market gateway, repository, and content-authority migration. Update it whenever a migration checkpoint changes live state, exposes a blocker, or closes a production-readiness gate. Architectural intent remains in `docs/project-architecture-migration.md` and `docs/package-ownership.md`; this ledger records execution status and evidence.

## Destination

The completed system has two operational domains:

- `treeseed-ai/platform` is the public Apache-2.0 installer and manager for customer deployments. It composes independent Admin, API, Agent, AI, CLI, Core, Reviewer, SDK, TreeDX, UI, fixture, and template repositories. It cannot check out or provision Market or Market API.
- `api.treeseed.dev` is the singleton gateway. Private Market API owns `/v1/market/**`; all declared hosted Admin routes pass through to a separately deployed public AGPL-3.0 Admin API service.
- Default deployments use the singleton URL for both control-plane and Market traffic. Sovereign deployments use a customer control plane for tenant data and the singleton gateway only for registry, commerce, licensing, and ecosystem governance.
- Every product project has an independently governed content repository. Git is durable history, TreeDX is the operational content plane, and immutable R2 releases are the runtime plane. Content repositories are never Platform workset checkouts.
- All development for all 31 repositories runs through a TreeSeed instance. Admin UI, CLI, interactive Codex, and autonomous agents use one API-owned proposal, decision, workday, capacity, assignment, checkpoint, receipt, stage, and release lifecycle. Local instances are supported control planes; unrestricted local source editing is not a supported development strategy.

The last item is the critical completion outcome. Repository migration, content separation, and provider reconciliation are enabling work, but the migration is not complete until the system can plan and implement a real multi-project initiative through agents and then fails closed when the same source changes are attempted outside an admitted assignment.

Codex is the default agent execution provider, with explicit policy-based overrides allowed. API is the sole durable action owner; CLI must expose every API action, UI must consume those same contracts, and agent tools must be assignment-scoped projections of them. Complete API/CLI parity plus a CLI-driven dogfood initiative are therefore completion evidence, not optional operator convenience.

## Resource Creation, Team Inventory, Workset, and Receipt Roles

The live team project inventory is the logical successor to the old parent-repository submodule map. A seed is only a resource-creation input:

1. A seed creates or adopts initial team resources; it does not remain current membership authority.
2. The live team project inventory resolves the Platform-managed software subset and repository bindings; exact Git commits are observed when a workset is materialized and frozen only in receipts.
3. `trsd platform workset` observes live branch heads and materializes exact commits as independent assignment-owned repositories. The directory layout is disposable custody, not developer authority.
4. `trsd save` creates a repository-scoped checkpoint by default.
5. `trsd save --federated` records exact pushed commits, dependency edges, contract digests, and remote proof in a `treeseed.integration-change-set/v1` receipt.
6. Stage and release consume verified receipts. Gitlinks are not promotion authority.

Governed agent execution now adds an authority layer before save: a reviewed assignment checkpoint produces a stable workset-local execution-authority receipt, and save embeds it only when repository identity, branch, and commit ancestry still match. This connects proposal/decision authority to exact source evidence without making local layout authoritative.

Cross-project coordination does not widen proposal authority. Every source-changing project in the unified Codex/CLI development path must have its own project-local proposal, accepted decision, planning and capacity-plan provenance, assignment graph, reviewed checkpoint, and receipt. Dependencies only link those independent authority chains and make downstream promotion fail when an upstream decision is missing, stale, or superseded. An umbrella objective may organize the initiative but cannot create assignments outside its project.

The final Codex/CLI path has one lifecycle: proposal, accepted decision, planning workday, estimate, accepted capacity plan, acting workday, project-scoped assignment, review, and receipt. The team project inventory supplies repository discovery and local materialization; it does not replace capacity admission. Direct ungoverned edits remain transitional legacy behavior and must not be considered staging-eligible after the unified gate is enabled.

Affected repositories are deliberately not frozen when a proposal begins. Planning workdays use the live team project inventory to discover dependencies and produce estimates. Accepted capacity plans and assignments freeze agent acting scope; an acting agent that discovers another repository must record the dependency and return it to planning rather than widen its assignment. Planning may expand read-only inventory inspection, but writable repository custody remains assignment-scoped. Exact repository promotion scope becomes immutable in the saved integration receipt and staging candidate.

The obsolete `treeseed.portfolio.json` authority path has been removed from SDK workset planning, managed-repository discovery, lifecycle fixtures, and generated Platform snapshots. The API now exposes an authorized team project inventory, and CLI workset planning converts that inventory into live-ref observations. Live local verification resolves exactly 13 Platform workset repositories: 12 managed primary repositories plus the unpaired fixture repository. Market, Market API, content repositories, and archived legacy template identities are absent.

The remaining transition is to stop using the Market workspace's compatibility gitlinks after active development moves to Platform worksets and the Platform snapshot can be promoted from reviewed federation evidence.

### Submodule-to-team-inventory transition map

| Capability | State | Remaining cutover |
| --- | --- | --- |
| Logical project bundle | Implemented; assignment adoption pending | The live team project inventory owns repository identity, role, content pairing, and integration branch after initial seed creation. |
| Exact integrated version | Current architecture | Portfolio snapshots and federation receipts own commits and contract digests instead of gitlinks. |
| Assignment-owned repository custody | Partial | Live team inventory and exact-ref receipts exist; bind materialization and mutation tools to admitted workday assignments and remove unrestricted developer entry. |
| Repository save | Implemented | Repository-scoped saves and federated receipts work; remove reliance on the compatibility parent checkout. |
| Stage authority | Implemented, promotion proof pending | Execute a reviewed real staging promotion using only a verified federation receipt. |
| Release authority | Designed, suspended | Restore only after reviewed OpenTofu topology and release guarantees permit it. |
| Legacy Market gitlinks | Transitional | Remove after Platform becomes the normal integration workspace and the filtered snapshot is receipt-promoted. |

The team project inventory is the canonical successor to submodule configuration. Its API/CLI live-ref path is verified and generated Platform history no longer contains a portfolio authority artifact. Verified workset repositories now fail federated save when a non-generated source path lacks project-scoped authority rooted at the exact inventory commit. The architecture is not fully cut over because the old Market checkout still provides compatibility materialization and Platform-root authority is not yet represented in the child workset receipt.

## Current Checkpoint

Updated: 2026-08-15

### Self-development transition integrity checkpoint

- The interrupted Agent Lab/UI campaign is recovered in the working graph. Workday terminalization now uses an idempotent API-owned admission fence, performs an authoritative durable reread, waits for terminal assignments/mode runs/settlements, and cannot report successful completion with a failed required assignment.
- Planning output registration now replays identical provenance and creates immutable proposal revisions only from the current base version; stale or divergent writes fail closed. Workday summary reads are aggregate-only by default and expose one explicitly requested, cursor-bounded evidence collection.
- The SDK operation registry now declares API descriptors, CLI actions, access, mutation mode, confirmation, UI availability, and agent visibility. Capacity-plan lifecycle, workday lifecycle, agent authoring/simulation, assignment recovery, and checkpoint integration are included; API descriptor parity is an executable contract rather than a handwritten claim.
- The canonical `agent.system` catalog now contains exactly fifteen dependency-ordered v2 capabilities: dynamic context, profile selection, Planning, Chat, Estimating, Acting, Reviewing, handoff, Reporting, recovery truthfulness, simulation isolation, Source golden, Research golden, project-fork portability, and Guide golden. All remain non-operational until three exact-generation semantic runs, including interruption/resume, advance each capability to active. Contract validation or process completion alone does not advance readiness.
- Chat remains non-operational. Its backend is migrating from direct Discussion-to-workday dispatch to TreeDX-before-invocation admission, hidden conversations, semantic communication/operation lanes, new-invocation continuation, bounded handoffs, semantic client actions, and approval-backed operation handoffs. Eight package-level v2 guarantees describe those dependencies, but they remain `planned` until live semantic evidence exists; no previous Chat run counts as success.
- Live diagnostic J is the first coherent required-response lifecycle, not activation evidence. Guide Steward produced an exact addressed question, closed and settled the first assignment as a released suspension, a human reply without a repeated mention created a new exact-lineage invocation and assignment, and that assignment produced a semantically correct durable response. Both assignments were reviewed and integrated only on isolated simulation refs; fresh read-back proved the canonical staging ref unchanged, one settlement per assignment, released custody, and zero assignment-scoped residue. The diagnostic included source changes and a managed API/operations-runner restart, so it cannot contribute to a same-generation passing streak.
- Execution mode is now structurally separate from environment. Simulation carries denied upstream authority through workdays, assignments, graphs, manifests, receipts, checkpoint integration, and save coverage; production assignments remain checkpoint-only and the reviewed platform path alone may issue exact-approved-ref authority. Governed agent/group deployment plans exact dependency closure, authors dormant project-local forks through the atomic TreeDX bundle, gates activation on exact evidence, and performs explicit three-way upgrades.
- A fresh local reconciliation after these changes exposed stale provider images, stale process source closures, two seed mutations, and missing Market CLI authentication. This is the current acceptance boundary: deterministic package builds and the fifteen-entry catalog are ready, but no live capability streak is credited until the managed generation converges and authenticated semantic campaigns pass.
- Platform workset receipts now bind team/project/decision/capacity-plan/workday/assignment/exact-base/expiry authority. Only the acting assignment's project may receive writable custody; the remaining inventory is exact-ref read-only materialization.
- The managed local platform now distinguishes `pause` from `stop`, reports per-repository upstream/dirty/divergence state, and computes a selected app's minimal restart closure.
- Designer/Simulation/Atlas now preserve dirty-overlay confirmation, immutable historical-definition evidence, validated return navigation, responsive layout, keyboard behavior, and forced-color behavior through shared UI contracts.
- Agent context is now an independently testable API-owned gate rather than an execution-time prompt convenience. Exact query and query-set revisions compile to bounded TreeDX requests, portable tests assert identities, relations, item/token limits, and latency, and durable checks retain only passing/failing/stale status, assertions, aggregate statistics, a result digest, and freshness—not the returned content. The readiness projection inventories explicitly unchecked definitions while exposing a separate passing-only selectable collection. The local operations runner periodically reruns initialized checks after their freshness window expires; deterministic maintenance authority prevents duplicate checks under repeated ticks. Workday preflight and demand admission both reject an agent reference without a fresh passing check. At execution, the agent rereads the exact query definition and executes it in real time through assignment-scoped TreeDX authority; the live result ref/digest/stats become assignment provenance and are never replaced by a cached test result.

This checkpoint is implemented and package-tested, but it is not an agent-readiness claim. An agent remains 100% broken until its exact semantic repository outcome is integrated, receipt-backed, settled, read back at the target ref, and residue-free. The live Agent Lab profiles must be rerun after the local stack is reconciled, and the governed SDK/CLI source canary must produce exact checkpoint/federation receipts.

The backend transition now includes canonical reusable context-query/query-set and instruction-template models, non-primary multi-group membership, discussion-topic subscriptions, per-model permissions, operational assignment plan/status/summary content, provisional artifact mutation receipts, receipt-gated signals, exact-definition performance attribution, and frozen group-proposal participation. These contracts are package-tested only. They have not yet produced the intended Guide repository outcome and therefore do not establish a working agent.

TreeDX authoring commits are now journaled in the control plane before API success across assignment proxy, Discussion, governance, knowledge review, and Agent Lab authoring paths. Publication records the corresponding integrated state. Failed, cancelled, expired, or semantically rejected completed assignments now have an authenticated `content-abandon` operation that exact-matches every unpublished commit, requires released/expired custody and simulated-human evidence from the owning workday, discards only `refs/heads/{assignmentId}`, journals abandonment, and proves ref plus journal read-back. Completed content with an integration receipt is categorically protected. Five obsolete pre-fix isolated communication diagnostics were cleaned through that operation. It categorically rejects `staging` and other non-assignment refs; the audit found older staging-lineage journal rows that require separate provenance reconciliation and must not be erased as if they were disposable branches. Local reconciliation continues to block rather than fetch over unresolved authoring state. Federated integration receipts now carry an explicit Platform-root exact-base and child-ref authority. Live interruption/resume and remote read-back remain required before either mechanism can be treated as proven.

### Self-hosting bootstrap

- The immediate destination is a production-shaped `treeseed.dev` Market + Market API gateway where a conversation can configure project agents through UI/CLI/API, run Codex-backed planning and acting workdays, preserve human decision authority, and produce TreeDX artifacts plus repository receipts.
- The local Market singleton seed now composes with the base seed's live team. It adopted the existing Market and Market API projects, replaced their primary and content repository authority from `knowledge-coop` to `treeseed-ai`, and reached an all-`noop` read-back plan without creating duplicates.
- Local TreeDX repository compilation now consumes the exact saved team seed set instead of hard-coding `treeseed.yaml`. The active four-seed inventory resolves 15 primary projects and imported the live `treeseed-ai/market-content` staging head into the Market TreeDX repository.
- Private `treeseed-ai/market-api-content` now follows the same exact-ref reconciliation through the existing sealed credential broker. The API verifies the live GitHub head with the central first-party authority, reconciles the project binding, and returns only a project/node/refspec/commit-scoped opaque delivery ID; TreeDX consumes it once and stores no reusable token. Live reconciliation synchronized all 15 projects, durable read-back showed the binding ready with `drift=none`, the delivery and grant consumed, and preparation/consumption audits present. A fresh local TreeDX plan is entirely `noop`.
- The `self-hosting-architect` definition is authored only through `trsd capacity agent-author`. Its current TreeDX staging commit is `94bcd52e91097a951fa7e46971c0661a5bbb4d69`, its planning profile declares the frozen `implementation` proposal type, and its evidence-heavy runtime budget is 300 seconds. No filesystem content write was used. A negative CLI probe rejected `activityProfiles.planning.execution.maxRuntimeSeconds: 0` with exact Zod and semantic diagnostic paths and made no commit. Replaying an unchanged intent returned the same commit, zero changed paths, and an explicit `idempotentReplay` receipt.
- SDK now owns portable Zod validation for the agent definition and every content-backed model in the SDK registry: governance, knowledge, people, agents, discussions, groups, and group edges. Model-aware create/update/link operations validate before TreeDX mutation, so invalid attempts leave no staged file; live Codex chat received exact diagnostics for missing `description`/`motivation` and invalid `status`/`proposal_type`, repaired them, validated the exact path, and committed once. Proposal readiness now consumes the same canonical `proposal_type` accepted by Zod. Proposal-type identifiers use one shared SDK/Core kebab-case contract instead of conflicting hard-coded enums; the project's frozen proposal-type inventory supplies the semantic allowlist. A portfolio invariant now fails if any future built-in content model lacks a portable schema, and malformed nested agent profiles return exact Zod fields such as `activityProfiles.planning.prompt.system` before TreeDX mutation. Remaining convergence work is limited to retiring overlapping Core/Astro declarations.
- SDK now also owns the portable Zod schemas for the three content collections outside its mutable model registry: agent tests, workday summaries, and template products. Core consumes those exact schemas through the bounded `@treeseed/sdk/content-validation` entrypoint. Live Market template and agent-test frontmatter pass; invalid workday values return exact `workDayId` and `startedAt` paths. All current content collections therefore have a Zod boundary; publication CI, generic TreeDX ingestion, and Discussion chat now consume it, while the remaining Core and bespoke-reader convergence stays explicitly tracked below.
- Discussion authoring and ingestion now use the same SDK schemas at the API boundary. User and agent chat records are validated before a TreeDX workspace is opened, and TreeDX records are validated before they are returned to Admin/chat. Invalid content returns `discussion_content_invalid` with exact diagnostic fields and cannot stage a changeset; focused API tests prove both the write and read paths, the locally reconciled API is healthy, and its repeated exact plan is `noop`.
- The shared Discussion UI now preserves failed optimistic messages and renders those exact diagnostic fields as Platform feedback in chat. Content publication also invokes the portable registry before runtime-manifest/R2 work, including pull-request `--validate-only` workflows, and serializes a stable JSON failure envelope. A read-only portfolio scan validated 644 known-model documents across the Market root and seven package content roots with no failures; SDK publication tests, API route/content tests, UI feedback tests, and all three package builds pass.
- The generic SDK TreeDX content backend now applies that registry to first-party models during every read and immediately before create/update mutation. Invalid ingestion fails with exact path/model/field diagnostics, malformed frontmatter is structured the same way, invalid direct reads cannot masquerade as missing records, and invalid writes make no TreeDX request. This audit also repaired direct `get()` paths that previously appended record names to an untrimmed `**` glob. Focused SDK TreeDX/publication coverage passes 29/29.
- Core now treats the SDK registry as its raw-frontmatter admission stage before Astro references, Starlight extensions, and runtime defaults. The same registry now guards API workday-demand ingestion, immutable planning proposal/feedback projection, versioned book/knowledge parsing, proposal-version changesets, and proposal discussion artifacts. This exposed and repaired the missing knowledge `bookId` registry binding/graph edge, canonized `knowledge-gap` as a valid question type, and replaced the old proposal-version plural-only source shape. The live/root/package/fixture scan now validates 646 known-model documents.
- Post-commit class synchronization refreshes graph and search indexes at the exact TreeDX commit, propagates that immutable ref into its read grant, rejects query failures instead of silently projecting an empty inventory, and excludes invalid definitions from runnable class reconciliation. A new `trsd capacity agent-definitions-author` SDK/CLI/API path validates and commits portable signal/proposal-type definitions through TreeDX. Live read-back proves the active self-hosting class at `94bcd52e91097a951fa7e46971c0661a5bbb4d69` with the `implementation` contract frozen into its handler refs.
- Live assignment `assignment_XMmAGWSq3szOvTLt43U1kdNcbw--4smL` completed the first production-shaped internal planning loop: assignment-scoped project materialization and TreeDX authority, Codex execution, field-addressable Zod repair before mutation, exact-path validation, one commit (`55e0e4aa6667407b0383665a5db6d10b59b6e800`), governance proposal ingestion, usage actuals, reservation consumption, ledger settlement, lease release, and concurrency-counter release. The durable proposal is `proposal:79ccce2e-8f14-4b60-afca-9b63abf57142:local-self-hosting-receipt-drill`. Workday run `06bfa0b7-8df4-4a7c-9410-64949f14a63b` then closed normally through the API operations runner with one completed assignment, one succeeded mode run, 175 active seconds, 192 elapsed seconds, and 65 released seconds.

### Live GitHub portfolio

- All 31 required repositories exist in `treeseed-ai` with `main` and `staging` branches.
- Public/private policy is applied: only `market-api` and `market-api-content` are private and proprietary.
- `information-hub` is absent from active repository policy and seeds.
- Templates use `template-engineering` and `template-research`.
- `TREESEED_GITHUB_TOKEN` is the sole first-party credential path.
- Repository reconciliation for the API project reached an all-`noop` plan after live read-back.
- Read-only staging smoke run `20260812164555` resolved `treeseed-ai/market` exclusively through `TREESEED_GITHUB_TOKEN` and passed all six pre-expansion checks.
- Isolated acceptance run `20260812165207` then passed 11/11 live capabilities: existing-repository adoption, default-branch bootstrap read-back, exact-SHA test branch creation, branch protection, Actions settings, environment branch policy, encrypted environment secret, repository variable create/update, workflow presence, workflow-run observation, and central-token resolution. The run used only `trsd-live-staging-github-20260812165207` resources and did not create, archive, or delete a repository.
- Cleanup run `20260812165154` was `noop` before acceptance. Acceptance removed its own prefixed environment, variable, branch protection, and branch; independent cleanup run `20260812165226` was again entirely `noop` with 11/11 checks and zero prefixed resources. Stateful recovery coverage proves cleanup removes protection before deleting an interrupted test branch and never deletes an unprefixed resource.

### Platform extraction

- Verified Platform snapshot immediately before this ledger update: main `a86a73ef3723ef20ff70e054929be611ce66637d`; staging `25ebbff2ab83117b27369a05316b2c508b9fdc3d`.
- The latest Platform refs are intentionally authoritative in `.treeseed/repository-migrations/treeseed-ai--platform--platform-workspace.json` and fresh GitHub read-back rather than copied back into this file: this ledger is itself part of the filtered Platform snapshot, so embedding its post-update target commit would be self-referential.
- Both branches have verified migration receipts and the extraction replay is all `noop`.
- A clean Platform clone passes `npm run verify`.
- A live workset materializes 13 exact, detached repositories and replays as 13 `noop` actions.
- The workset contains no `.gitmodules`, Market, Market API, or content repository.
- Platform configuration fixes the external Market profile to `https://api.treeseed.dev`, defaults to `market-passthrough`, and declares no hosted singleton resources.

### Repository federation

- Latest verified full federation before this ledger-only checkpoint: `3768153910c1bedb3986d18d916828a4441418035c8f7d5de55dd7c5aac83340`; root `12918d30e5437cbfa22ca87ba283ade81730d9a5`; 14 repositories with fresh remote proof. SDK authority implementation: `26551496454f36a32dfbbb92d328ca967e8a1e08`; CLI reporting: `5a9b5c0aa5bd3e6098bbf99f7728c88004634c4d`.
- Latest verified full federation after the API runtime-plane gate: `9239393aed7436a965c5a0321ab5baff5f2ac26e649c5c9e4d4866e7c6586c0f`; root `cd599155ba61cfbdb0d8d79d92d1fd2314710365`; 14 repositories with fresh remote proof.
- Latest verified full federation before the ledger-only checkpoint: `acc41b230fc5f37d22bc5fb6c05e713f0518b4210e1dc33854b9ec070b8edf02`; root `0d4c3e94de21d17953876aad15930c468150a305`; 14 repositories with fresh remote proof.
- Latest verified full federation before the singleton gateway reconciliation: `2e3726da4242bfb170862911150f6d6be36b86ebac531db25a61ed8d91739178`.
- Transitional integration root ref at that checkpoint: `ee852d4b5cdd94184b9b22497535e159abc8254b`.
- Receipt scope: `federated`; repository count: 14; every remote proof verified.
- Market API and content repositories are absent from the receipt checkout graph.
- Repository-scoped saves cannot stage; stage verifies a federated receipt against live refs; release verifies staged refs.
- Save, stage, close, release-plan, interruption, retry, and no-gitlink lifecycle scenarios pass.
- Supervised checkpoint integration now emits `treeseed.governed-execution-authority/v1` evidence with proposal/decision, assignment graph, deliverable, repository, exact refs, changed paths, and optional cross-project decision dependencies. Repository and federated saves include matching authorities in the hashed integration receipt after ancestry validation.
- Focused SDK checkpoint tests pass 5/5, and the full save/stage lifecycle file passes 15/15 with authority embedding, content-derived receipt integrity, remote-ref consumption, fail-closed validator absence, and successful control-plane revalidation. Complete governed-diff coverage is not yet a stage gate.

### API licensing

- `treeseed-ai/api` is public and GitHub recognizes `AGPL-3.0`.
- Source metadata is `AGPL-3.0-only`; `COMMERCIAL.md` describes the alternative commercial subscription license without misrepresenting AGPL commercial-use rights.
- Contributor provenance and a contributor-grant workflow are present.
- `api-content` remains public Apache-2.0.
- License migration replay is `noop` on `main` and `staging`.

### Agent Lab workspace and forensics

- `@treeseed/ui` now owns the reusable `WorkspaceFocusSurface`, focus controls, bounded overlay coordinator, overlay stack, linked diagnostic table, and evidence diff presentation contracts. Atlas, the Agent Designer, rich Markdown editor, YAML/source editor, and Simulation Bay consume the shared focus surface instead of remounting content into separate pages.
- Agent Lab vital density is an authenticated, team-scoped Atlas view-state field with a one-time browser-storage migration and a browser-only availability fallback. Atlas focus is separate URL state (`?focus=atlas`), with temporary `?research=1` compatibility, and is deliberately not restored as a future-visit preference.
- Atlas exposes Easy and permission-gated Diagnostic modes, exact inspect history, protected assignment graphs, focused content-region layout, and overlay-preserving playback behavior. SDK-owned Zod contracts cover forensic activity queries/pages, replay requests, and transcript payloads. API and Agent expose assignment-scoped machine evidence through an authorized endpoint and `treeseed.assignment_activity`; CLI forensic output remains JSON/JSONL validated against the same contracts.
- Atlas, Command overlays, Agent Designer, the source editor, Simulation Bay, and the shared coordinator now consume one UI-owned URL/history adapter. It enforces a single focused surface, an eight-layer overlay bound, canonical `focus`/`inspect` parameters, overlay-first close semantics, and compatibility decoding for existing Command links instead of maintaining island-specific history mutations.
- Local scene run `20260812t164305372z` passed sign-in, active-team selection, Atlas availability, compact vitals, focused Atlas URL/layout/Shrink, Escape shrink, and expanded-vitals restoration after the shared-navigation migration. Every step completed with zero console or network errors. Captured evidence is under `.treeseed/scenes/runs/agent-lab-workspace-focus/20260812T164305372Z-20260812t164305372z/`.
- UI focus state now has one reusable history owner consumed by Agent Designer, structured Markdown/YAML source editing, rich Markdown/MDX editing, and Simulation Bay. Focus changes update canonical URL history without remounting the application, while Simulation Bay retains its `return` cleanup contract. Component acceptance keeps the same Designer, source-editor, and rich-editor nodes, uncommitted values, and edited repository path across focused mode and a browser-history transition (11/11 focused tests).
- The Agent Designer now follows `design/agent-manager.html` through reusable `@treeseed/ui` shell, identity/common-context, activity-profile, and simulation-setup components. The discarded anatomy/body-part metaphor has no active source, styling, label, or guarantee path. Editable fields are limited to the current SDK Zod definition and API authoring contracts; design-only autosave/publication, stored TreeDX queries, provider mutation, performance recommendations, and isolated single-agent execution are not fabricated.
- Designer draft persistence is revision-bound and preserves MDX body, selected section, structured definition, and exact return location across Designer → simulation setup → Simulation Bay → Designer. Focused component coverage passes 9/9 across the shared surface/overlay and editor round-trip suites; package type/Astro validation reports zero errors and the production UI library builds successfully.
- Live Admin inspection confirms the acceptance team renders the integrated Build and Simulation Bay surfaces. A true repository-backed Designer scene is still blocked for that fixture because its project inventory uses an acceptance placeholder repository with no exact TreeDX authoring ref and currently has no approved active capacity provider. No sample definition was injected to conceal this integration gap; a repository-backed team/fixture and exact ref must be reconciled before the live Designer-to-workday acceptance can be claimed.
- SDK scenes now expose a typed `browserHistory: back|forward` action, so Back/Forward behavior is verifiable through the same CLI/API simulation path instead of being approximated with Escape. The forensic observer classifies a controlled `EventSource` `ERR_ABORTED` as stream teardown while retaining ordinary fetch aborts as errors. Focused SDK scene coverage passes 19/19 across the action/parser/build and evidence-classification suites.
- Real-shell scene `20260812t170331796z` passed all 12 authenticated steps, including compact vitals, focus/Shrink, Escape, a second focus, real Chromium Back, exact inline restoration, and saved vital-density restoration. The run contains zero failed steps, console errors, or network errors; evidence is under `.treeseed/scenes/runs/agent-lab-workspace-focus/20260812T170331796Z-20260812t170331796z/`.
- The expanded acceptance circuit now passes 19/19 at desktop (`20260813t160259380z`), tablet (`20260813t160214573z`), and mobile (`20260813t160043223z`). Each real-browser run proves expanded/compact vitals, bounded event paging and filtering, exact historical/live mode transitions for the selected circuit, Atlas focus, Escape shrink, browser-Back shrink, and restored vital density without console or network errors. Targeted mobile visual audit `20260813t155-9c58f1d8` reports HTTP 200, a 393-by-851 viewport, no document overflow, no client errors, no default-styled controls, no visible semantic error surface, and zero findings.
- This is an implemented and locally accepted foundation, not completion of the complete viewer plan. Unsaved-change confirmation on overlay exit, real simulation-to-workday-to-Atlas/Return-to-Designer acceptance, zoom/forced-color/accessibility coverage, immutable historical-definition retention proof, and external viewer retirement remain open.

### Control-plane and singleton gateway

- Public Admin API feature ref `a2cfc3b65b6f0651cde928345865a3ad24d252de` removes the first singleton-owned route from Admin. The generated 578-route artifact currently reports 429 Admin routes, 149 Market routes, and `migrationReady: false`; full local verification passed 505 tests with 4 intentional skips plus the isolated acceptance matrix. Repository receipt: `3874379fc6d64e6654acf426d618d88960530bb74d00a398350c92e7507ab306`.
- The Platform compiler supports `market-passthrough`, `external`, and `managed` modes and rejects inconsistent explicitly declared topology. Managed mode requires API, database, operations runner, and public TreeDX federation; pass-through and external modes reject those Platform-owned control-plane resources.
- Market operations remain fixed to the immutable `treeseed` singleton profile and customer plans cannot declare a Market API service.
- SDK feature `4abb2dfe0f18b7cfe8636c405cb177501bc03ced` makes bounded overlay comparison byte-exact after the first Market route extraction; its repository receipt is `7246ae7014d60b56f2480bb3e472fa83427a6265b0eb0f31915f962eb72c542d`.
- Bounded SDK staging gateway contract: `d8d1a1c52303128f7c9d8a49863d4b280b75a77b`; bounded CLI staging migration contract: `cbb3044ea90f78ea55755fd50d2f58317b2a3437`; both replay as `noop`.
- Bounded Admin staging runtime/descriptor contract: `5e9b323a597b7d148d77b1172ffc894ef8e41e42`; replay is `noop` against the exact SDK staging ref.
- Private Market API main: `efe56ed753f26b037c98fbbcfb2b3f02da8a0f11`; staging: `9193fbd809f11708914475cb52bffd6147782e74`; both replay as `noop`.
- The singleton manifest pins Admin API `5e9b323a597b7d148d77b1172ffc894ef8e41e42` and descriptor `sha256:810cccc26feaf12fe437e102274b78878d4a420325c666b418ed297bcdca3f8e`.
- A clean private staging clone passes `npm ci`, TypeScript build, and all six gateway/profile/descriptor tests. The canonical `GET /v1/market/profile` is now implemented directly by the singleton and is absent from Admin. The managed lockfile pins the exact SDK commit and CI uses the lockfile.
- A built-server acceptance run against a disposable local Admin upstream proves exact descriptor routing, HTTP `429` and structured body propagation, two independent `Set-Cookie` headers, rate-limit propagation, internal-secret stripping, signed identity forwarding, and a real `101` WebSocket upgrade through the generated private server.
- SDK transport coverage proves streaming request/response bounds, immediate SSE delivery, client cancellation distinct from timeout, contained assertion failure, hop-by-hop removal, exact method/path admission, WebSocket route rejection, bidirectional socket data, and structured upgrade timeout. The complete fast suite passes 1,394 tests across 367 files.
- SDK feature `57c333a3d8ceb38dcf60dbbfe41c427a5fdf6ff5` and repository receipt `984c7dbc4014d631741cea5f1d19bfbb94e3c93f169c74d54c75c287d93552c9` separate capacity-provider `TREESEED_MARKET_API_BASE_URL` from the resolved `TREESEED_API_BASE_URL`. Pass-through, external, and managed reconciliation cases pass; the bounded staging overlay includes the resolver and test.
- This is repository and contract acceptance only. No hosted Railway or Cloudflare deployment occurred.

### Content repositories

- Platform-managed content history audit currently reports all 38 observed branch migrations as `noop` across 13 projects.
- `ui-content` feature history was created and verified at `329d9e8b74e7078a1cc1f20201a484236ebcb84b`.
- Admin staging content cutover is verified: source and target tree `a9e3c0520a9aba9a19b49aa2ba5c9721c36f1510`, R2 publication verified, TreeDX ref verified, and the software path removed.
- Admin feature history now replays as `noop` after software-path removal. The history planner requires the exact verified cutover contract, project/repository/path identity, Git tree digest, R2 verification, and TreeDX verification, and preserves the original authoritative path in its journal.
- Singleton Market content migration is declared separately in `seeds/market-singleton.yaml`; its independent live replay is 5/5 `noop`. Market feature content is verified at `76cce13fa82763a2edf19987d8d62f550b5dd72c`, while private proprietary Market API content remains isolated from the public content plane.
- History verification does not by itself authorize software-path removal; TreeDX, R2, runtime, and cutover receipts remain mandatory.

### Local runtime and verification

- The local isolation target is now explicit: the persistent Platform instance holds the complete active team inventory, deployment profiles enable only required services, and admitted assignments materialize only selected primary repositories plus exact transitive dependencies as independent worktrees.
- Provider-managed containers consume assignment custody without widening it. Optional authenticated, assignment-qualified Cloudflare previews must record an exact source closure and reconcile their DNS, access policy, expiry, verification, and cleanup without using singleton Market identities.

- Integrated local reconciliation rebuilt the exact agent manager/runner images and reconciled PostgreSQL, TreeDX, API, Market web, seed bootstrap, both capacity providers, and the Cloudflare connector.
- API replay converged with every selected unit ready and verified after correcting its source-closure receipt.
- Hosted deployment remains suspended. No push-triggered hosted deployment or production release was restored.
- Root tests: 102 passed.
- CLI tests: 200 passed.
- SDK lifecycle tests: 55 passed.
- SDK fast suite at the cutover-replay checkpoint: 1,384 passed across 366 test files.
- API suite: 503 passed and 4 skipped.
- File-size, architecture, and TypeScript-source policies pass.
- The live local team inventory now omits two reversibly archived legacy template project records, retains their repositories, and resolves the renamed templates under `treeseed-ai`.
- Codex subscription execution (`codex-sub`) is the rendered default for the local agent provider. Its grants cover all 15 primary projects: 13 Platform-managed plus the separately governed Market and Market API projects. Their content bindings bring the managed portfolio to 31 repositories.
- The provider now advertises that default in its live availability snapshot. API supply selection honors it after health, capability, reliability, pressure, and explicit team policy, preventing an alphabetically earlier provider from winning an otherwise equal assignment before the runner sees it.
- Agent and platform seed reconciliation now share the active team project inventory and the same deterministic allocation set. Live `agents`, `platform`, and repeated replays all returned `seed-allocation-4f931c26d45d9a413f6df5ca`; neither seed supersedes the other's capacity policy.
- A live workset plan resolves exactly 13 Platform repositories from API inventory and exact GitHub refs. Its blocked actions are expected because this transitional Market checkout is dirty or intentionally at different commits; three missing disposable paths are correctly planned as creates.

## Phase Progress

| Area | Status | Exit condition |
| --- | --- | --- |
| Architecture docs and seeds | In progress | Ledger and canonical docs agree; Market singleton and Platform seeds remain disjoint. |
| GitHub repository reconciliation | Core lifecycle live-proven; portfolio replay pending | Apply the proven lifecycle across the complete public/private portfolio and require final `noop` policy plans. |
| Central GitHub authentication | Implemented and live-proven on first-party authority | Extend redacted central-token evidence across the complete 31-repository portfolio. |
| Standalone Platform root | Live inventory cutover proven | Bind writable custody to admitted assignments and remove transitional Market gitlinks. |
| Receipt-based federation | Implemented | Stage consumes the reviewed receipt in a real promotion and repeat execution remains `noop`. |
| Governance-to-receipt provenance | Workset source gate implemented; root/legacy cutover pending | Add Platform-root base authority, retire the gitlink compatibility path, and prove every source-changing repository through a real planning/acting workday. |
| Control-plane modes | In progress | Typed routing and explicit topology validation compile; full managed reconciliation and sovereignty acceptance remain. |
| Market gateway/Admin pass-through | In progress | Repository-level HTTP, cookie, SSE, WebSocket, timeout, cancellation, size-bound, error, idempotency, rate-limit, descriptor, health/readiness, and clean-clone acceptance pass; hosted integration remains suspended and unproven. |
| Market/Admin UI split | Not complete | Admin contains no Market implementation; Market owns commerce and ecosystem-governance routes. |
| Routed SDK transport | Not complete | One client routes Market and control-plane methods correctly in every control-plane mode. |
| Sovereign migration | Not complete | Journaled export/import/digest verification and atomic configuration switch pass without commerce-data migration. |
| Licensing | API implemented; portfolio pending | Provenance checks and correct licenses are complete for every public/private repository. |
| Content authority | In progress | Every pair has verified history, TreeDX binding, immutable R2 staging publication, runtime proof, cutover receipt, and no software publication workflow. |
| Singleton deployment authority | Designed, suspended | Protected manual staging/prod workflows exist only in private Market API after OpenTofu review lifts suspension. |
| Provider acceptance | In progress; GitHub lifecycle passed | Cloudflare, rebuilt Railway, and local acceptance plus cleanup pass; every final plan is `noop`. |

## Production Blockers

1. Hosted Railway/Cloudflare topology remains intentionally suspended pending reviewed OpenTofu design.
2. Market API gateway transport is live and independently verified at repository and local built-server boundaries, but hosted Railway/Cloudflare integration remains suspended and unproven.
3. Market/Admin UI and API ownership extraction is incomplete.
4. Customer control-plane modes and sovereign-data migration are incomplete.
5. Content cutover remains incomplete across the portfolio, although the Admin cutover-aware replay gap is closed.
6. Stable Platform promotion is still derived from stable branch heads rather than a reviewed feature federation receipt.
7. The core GitHub lifecycle has isolated live acceptance and repeated zero-residue cleanup evidence; full 31-repository policy replay and Cloudflare isolated acceptance remain outstanding.
8. Railway project/environment resolution must be proven against the rebuilt topology before hosted readiness can be claimed.
9. The public Admin API descriptor still contains 149 Market-owned commerce, catalog, registry-seed, commons-governance, and template routes. The former Market-profile route is now singleton-owned. Each remaining route carries an explicit `runtimePlane`, and the built descriptor reports `adminRouteCount`, `marketRouteCount`, and `migrationReady`; sovereign migration apply must remain blocked until `marketRouteCount` is zero and those routes are owned by the private Market API.
10. Every current content collection has Zod validation, all agent-mutable SDK models validate before TreeDX mutation, the generic SDK TreeDX backend validates all first-party models on read/write, Core performs portable admission before Astro adaptation, Discussion validates on write/read and renders exact fields, portfolio workflows validate through publication reconciliation, and workday, governance, knowledge, Agent Lab, and contextual relation-search paths use the same boundary. Live Codex agents have received exact invalid-enum feedback and corrected their content before mutation. End-to-end Agent Lab completion evidence is still required before the guarantee can become active.
11. Timeout cancellation, telemetry-file readiness, successful artifact commit, assignment completion, usage actuals, reservation consumption, ledger settlement, counter release, and one complete scheduler-driven workday closure are now live-proven. Repeated closure plus multi-agent/multi-project coverage remain required before the capacity lifecycle is production-ready.
12. Assignment workspace provisioning preserves TreeDX base commit/ref and rich project/capability context, and live model-aware create/validate/commit is proven. Local reconciliation still reports unpublished TreeDX authoring refs as drift against GitHub, so receipt publication/resumption must be modeled before restarts are safe.
13. `trsd dev restart --app api` currently selects broader local dependencies than its focused CLI contract implies and can reconcile all local TreeDX projects or rebuild unrelated provider images. Exact unit-scoped reconciliation is bounded, but the developer-facing app filter must compile and prove the same minimal dependency closure before targeted restart is considered safe.

## Issues and Incident Log

### Seed existence was mistaken for live authority

Early migration work could describe repositories that did not yet exist. The correction was to create/adopt all live GitHub repositories early, verify their branches/visibility, and make provider read-back authoritative. Seeds are desired configuration, not evidence of existence.

### Save invoked from a package selected the Market root

CLI cwd resolution promoted `save` to the nearest tenant manifest. Package saves could therefore sweep the parent workspace. The CLI now preserves the invoking repository for `save`, while tenant-scoped commands still resolve the project root. A regression test covers nested repository invocation.

### Unrelated design work entered a federation checkpoint

The cwd defect temporarily captured `design/agent-manager.html`. Forward corrective commits removed it; the exact user version is preserved as the sole local modification and is not present at the current remote feature head.

### Supervisor update raced restoration of a local edit

The persistent Platform supervisor began `trsd update` while the root was clean, then `commitRootUpdateIfNeeded` used `git add -A` after a local edit appeared. The update commit swept that edit. The SDK now commits only reconciliation-owned managed paths with `git commit --only`, returns preserved paths, and has a regression proving concurrent operator edits remain uncommitted. A forward correction and replacement federation receipt were issued.

### Raw Git output changed license digest interpretation

Leading spaces in the canonical GNU license were treated as output formatting and caused false drift. Raw content reads were corrected; repeated API licensing reconciliation is now `noop`.

### Local platform stop was restarted by systemd

`trsd platform stop` terminated the supervisor process, but its enabled user unit restarted it. Work proceeded under workflow locks and dirty-worktree fail-closed behavior. The stop contract still needs explicit systemd persistence semantics and acceptance coverage.

### Protected acceptance branches initially defeated cleanup

The expanded GitHub acceptance correctly set `allow_deletions: false` on its disposable branch, but the first implementation tried to delete that protected ref directly during recovery. Cleanup now removes the exact prefixed environment and variable, removes branch protection, and only then deletes the exact prefixed ref. Stateful regression coverage rejects repository deletion and preserves all unprefixed resources. Live acceptance and a separate final cleanup both left zero resources.

### First local restart recorded a pre-build API source digest

The integrated restart rebuilt dependencies after computing the first API closure. API remained healthy but read-back reported digest drift. An API-only replay converged. Reconciliation must continue to require post-build read-back and idempotent replay.

### Content-history apply initially lost replay context

The first cutover-aware plan correctly retained Admin's original `docs/src/content` path, but after apply the source receipt pointed at a commit where that path no longer existed. Replay therefore lacked a readable prior tree and blocked. Classification now uses the immutable cutover tree as the authority after removal, requires the live target tree to match it, and treats an available prior tree as an additional equality check. Apply preserves the original path in the migration journal; live Admin apply and the subsequent 38-branch portfolio plan are entirely `noop`.

### Generated Market API repository was live but not reproducible

The first private repository reconciliation reached `noop`, but a clean clone could not run `npm ci` because the generated overlay had no lockfile. The workspace reconciler now generates one lockfile per changed plan from the exact SDK ref, verifies npm v3 structure and manifest parity, manages it through the singleton manifest, and reuses immutable receipts for network-free replay. Unused direct dependencies were removed.

### Bounded SDK gateway migration omitted its public export

After the lockfile fix, a clean clone installed but could not resolve `@treeseed/sdk/market-gateway`. The bounded staging overlay had copied implementation files without `package.json` and `package-lock.json`. Those package contracts are now part of the gateway migration inventory and have regression coverage.

### CI suppressed the pinned SDK build

The next clone had the correct export but `npm ci --ignore-scripts` suppressed the exact Git SDK dependency's required prepare/build step, leaving no `dist/gateway` export. Generated CI now uses `npm ci`; lockfile generation continues to use `--ignore-scripts`. Clean-clone build and five tests pass. npm reports 12 transitive audit findings (1 low, 1 moderate, 9 high, 1 critical); dependency provenance and upgrade impact require a separate bounded security audit rather than an unreviewed lock rewrite.

### WebSocket support was declared but not bound to the generated server

The HTTP proxy exposed an injectable WebSocket callback, but the generated Node singleton server never handled the `upgrade` event. Real clients therefore had no operational pass-through despite interface-level tests. SDK now owns a descriptor-gated Node WebSocket bridge with shared header/secret policy, assertion forwarding, timeout and cancellation behavior, raw multi-cookie handshake propagation, and bidirectional socket piping. The generated private server binds it explicitly. A real local handshake and echo path passes through the built clean clone.

### Managed control-plane configuration is ahead of its lifecycle command

The typed deploy configuration and generic reconciler already distinguish `market-passthrough`, `external`, and `managed`, reject customer-owned Market resources, and can compile API, PostgreSQL, operations-runner, public TreeDX, DNS, secrets, and runtime variables. The audit found no journaled `trsd control-plane migrate` workflow yet. Capacity-provider deployment variables now preserve the central Market URL separately from the resolved control plane, including root-managed API domains. Agent provider manifests and persisted connection state still use the legacy `marketUrl` name for control-plane connectivity; that terminology and end-to-end protocol wiring must be migrated atomically before sovereignty guarantees can become active.

### Admin route inventory still contains singleton Market behavior

The route audit found that the artifact named `admin-api-descriptor.json` still inventories `/v1/commerce/**`, catalog, registry seeds, templates, commons governance, and Market profile routes from the public API repository. The private Market API is currently a verified gateway over that full descriptor, not yet the owner of those implementations. Enabling sovereign export/import in this state could migrate commerce data into a customer control plane. Route descriptors now classify every route as `admin` or `market` and publish split counts plus a fail-closed migration-readiness bit. The Market API must adopt the Market-classified implementations and persistence before the Admin export contract or migration apply can be enabled. Admin-facing `/v1/ui/**` projections remain on the Admin plane because they expose project governance and operations rather than Market implementation.

### Receipt graph refreshes cascade by exact dependency

Federation advanced CLI dependency pointers after the first gateway staging overlay. Gateway reconciliation correctly reported bounded CLI drift, advanced CLI staging to `da8bf211fa65410c1a5d4bb855703af753c954c4`, and replayed as `noop`. Platform then detected that exact downstream ref change and advanced only its staging snapshot to `bd9fa82a901dfa66ec8758547398749d8e5fb3cc`. This is expected receipt-graph behavior and demonstrates why a final plan must be taken after all upstream receipts settle.

### Bounded overlay comparison trimmed authoritative source

The first Market-profile extraction correctly updated Admin staging, but an immediate plan reported perpetual drift. The Git read helper normalized output with `trim()`, while `sdk-route-map.ts` intentionally began with two blank lines; comparison therefore discarded authoritative leading bytes. Both bounded overlay comparators now request byte-preserving Git output and compare exact content. SDK, CLI, Admin, and private Market API were reconciled again in dependency order, and every final plan is `noop`.

### Runtime seed reconciliation stopped seeing its own history

After many capacity-policy revisions, the local runtime seed reconciler requested a nominal limit of 200 but the API returned paginated sets capped at 50. The reconciler did not follow `nextCursor`, could no longer find its earlier deterministic allocation, and correctly hit an idempotency-input conflict instead of mutating ambiguous state. SDK reconciliation now exhausts all grant and allocation-set pages before diffing. The focused pagination regression passes, and retrying the unchanged `trsd seed agents --apply` recovered successfully.

### Local TreeDX ignored selected team seeds

The persistent runner correctly saved `treeseed`, `agents`, `platform`, and `market-singleton`, but local TreeDX repository compilation always read only `seeds/treeseed.yaml`. Market content therefore lacked a local staging ref even though GitHub and the API binding were correct. TreeDX inputs now compose the exact selected seed set, use persisted selection when a caller omits it, deduplicate identical declarations, and fail closed on conflicting project identities. Focused SDK tests cover selection and conflict behavior.

### Agent authoring validation and synchronization had split authority

The CLI plan path compiled agent frontmatter without validating it, API authoring used a custom activity-profile validator instead of Zod, and post-commit reconciliation reread an older base ref. SDK now exposes a portable Zod agent schema, CLI and API validate the complete compiled document before mutation, diagnostics retain exact nested paths, and class synchronization consumes the exact commit receipt. Live projection initially remained empty because TreeDX extensions require a leading dot, queried definition paths exceeded the minted token paths, and the inventory helper dropped the immutable ref when opening its own connection. Those contracts are aligned and query failures at an immutable ref now fail closed. During the audit, `primaryGroupId` was also found in compiled documents but absent from the strict Astro schema, confirming that the remaining collection schemas need one portable owner.

### TreeDX changesets reversed multi-line additions

The unified-patch accumulator prepended additions in forward order and then reversed the full accumulator, reversing every added multi-line document. The patch application now composes additions in the accumulator's expected order, and the controller regression asserts exact byte order for a multi-line frontmatter document. The focused TreeDX controller suite passes 3/3.

### Local content reconciliation discarded an unpublished authoring receipt

Restarting the managed stack fetched the GitHub staging ref and force-updated the local Market content branch, replacing a valid but unpublished TreeDX authoring commit. The definition was recoverable from its CLI intent and was recreated, but this is not an acceptable steady-state lifecycle. Pending TreeDX commits must become journaled publication state that reconciliation preserves and resumes, or authoring must publish durably before returning success. Until that is fixed, a local restart can supersede unpublished content receipts and must not be treated as safe publication evidence.

The later scoped API restart failed closed instead of overwriting commit `48c81b797f34d6f273a66df7472ed19032f3b094`, reporting its divergence from GitHub staging. Preservation is safer, but local reconciliation still classifies an active authoring receipt as drift. The desired-state model must explicitly distinguish unpublished governed development refs from stale provider state.

### Workday assignments lost agent source authority at runtime

Agent-class reconciliation retained the TreeDX authoring commit, but demand compilation initially replaced it with local `refs/heads/main`; after that was corrected, the provider agent-spec loader still ignored the issued workspace and read the repository default ref. Planning snapshots now freeze the class's immutable source ref, demand compilation rejects mutable projected refs, and the provider loads the definition through the assignment workspace. Live run `947e9e55-08af-44d5-9e84-cdd0bffb052d` proved one exact agent definition loaded at `48c81b797f34d6f273a66df7472ed19032f3b094` before AgentKernel and Codex execution.

### Provider project isolation still depended on the container working directory

The first repaired workday reached project materialization but runtime plugin discovery called tenant-root resolution from `/app`. Agent runtime registries are now keyed by explicit project root and plugin loading receives that same root. A focused provider test passes, and the next live workday progressed beyond runtime discovery without a tenant-root fallback.

### Timed-out Codex execution outlived its assignment lease

The first Codex-backed self-hosting run reached proposal authoring, then exceeded its 60-second profile. The execution lifecycle released the assignment lease, but the Codex thread continued to emit messages and attempted further tools. This is a fail-closed blocker, not a reason merely to increase the profile: timeout cancellation must terminate the provider invocation and close tool access before assignment settlement.

Cancellation now reaches the Codex provider and prevents late tool events. Subsequent live runs exposed three independent completion defects in sequence: invalid content was staged before validation, proposal readiness ignored the canonical singular `proposal_type`, and proposal ingestion looked up the API run with the child project-envelope ID. Those owning boundaries now validate before mutation, normalize the canonical proposal field, and resolve the API run from durable assignment metadata. Focused SDK, Agent, and API regressions pass. The final live assignment completed and settled; the longer 300-second profile is retained because the required evidence search, schema repair, exact-path verification, and receipt reporting legitimately exceeded 180 seconds.

### Portable proposal-type contracts lacked a CLI authoring path

The self-hosting class could create a Zod-valid `implementation` proposal, but its immutable workday initially had no proposal-type contract because Market content only declared editorial types. The API already supported validated authoring bundles, while SDK/CLI exposed only single-agent authoring. `trsd capacity agent-definitions-author` now plans or applies a validated portable definition bundle through the same API and TreeDX changeset path. The `implementation` contract was authored at commit `678b597c4f78243d121a395116a7b934d19e9250`, and the agent was re-authored so its planning intent and frozen contract agree.

### The default local platform omitted its operations runner

The first successful assignment settled, but its parent workday remained running past the deadline and grace period. The API maintenance tick was correct; the default local desired-state graph contained only Market web and API processes even though the documented platform contract included the operations runner. The runner is now an explicit SDK reconciliation resource dependent on the API, and default local startup selects it. A focused `--app operations-runner` recovery path reconciles only the runner and its dependencies, avoiding unrelated TreeDX content drift. Live readiness passed at port 3001 and the runner terminalized workday `06bfa0b7-8df4-4a7c-9410-64949f14a63b` without direct database or API mutation.

### Private content fetch lacked a credential delivery

Exact team-seed composition exposed `market-api-content` to local TreeDX for the first time. GitHub read-back succeeded with the central token, but the TreeDX Git transport correctly rejected an unauthenticated fetch of the private repository. The SDK now requests a bounded delivery from a platform-runner-only API route; the API verifies repository metadata and the exact head, reconciles the binding and first-party central authority, and returns only an opaque ID for TreeDX consumption. The shared delivery helper recovers interrupted grants, tolerates concurrent idempotent attempts, reuses only ready unexpired deliveries, and creates a new attempt after consumption or expiry.

The first live retry returned `404` because a persisted team key in `team:treeseed` form was treated as a slash path; the parser now accepts both canonical separators. The second returned an explicit authority error because the route received public runtime configuration instead of the server process environment; credential resolution now stays server-side. The third synchronized the private repository, then all 15 local projects replayed as `noop`. Read-only database evidence confirmed central authority, a ready binding with no drift, one consumed sealed delivery/grant, and separate preparation and node-consumption audit events. No credential value entered command output or the ledger.

### Targeted API restart selected unrelated reconciliation work

While deploying the credential preparation route locally, `trsd dev restart --app api` also selected the local TreeDX portfolio and rebuilt an agent image. The documented and tested app-only expectation is narrower. An exact `trsd reconcile apply --unit-id local-process:api` applied only the API and required dependencies, so the engine can express the correct boundary; the CLI dev-selection compiler still needs regression repair before targeted restart is trusted for routine internal development.

### Provider seeds competed for one team allocation

The agent and platform provider seeds each built an allocation from only their own grant list. Because the API correctly permits one active allocation set per team, applying one seed superseded the other's otherwise healthy set and repeated reconciliation could never converge. Allocation ownership now follows the active, non-archived API team project inventory while grants remain provider-specific. Matching active policy is adopted instead of superseded. Live agent, platform, agent, platform application returned the same allocation ID on all four runs, and the portfolio seed remains 29/29 unchanged.

### Rebuilt provider images did not replace running containers

The image units rebuilt successfully after Agent source changed, but the dependent Compose unit's desired spec omitted the source-closure digest. It therefore remained `noop`, and the live manager continued advertising the old runtime behavior. Provider Compose resources now include the same source-closure digest as their image and capacity units. The regression proves the digest is compiled into desired state; live reconciliation updated both provider Compose projects, and fresh availability read-back marks only `codex-sub` as preferred.

### Legacy template projects remained in live inventory

Seeds correctly stopped declaring the old `treeseed-templates/engineering` and `treeseed-templates/research` projects, but seeds are creation inputs and do not delete ongoing team inventory. New audited `projects archive|restore` API/SDK/CLI operations provide reversible inventory lifecycle without repository deletion. Both old project records were archived through the CLI, are absent from inventory, and remain recoverable.

### TreeDX converted empty YAML sequences into strings

A production-shaped Agent Lab schedule rejected a valid objective because TreeDX returned `related_questions: ""` for source frontmatter containing `relatedQuestions: []`. The Elixir normalizer treated the empty list as printable character data and stringified it. Empty lists now retain sequence identity before printable-character normalization; the repository-query controller regression proves `relatedQuestions: []` survives projection, the focused TreeDX suite passes 5/5, and exact local TreeDX reconciliation rebuilt the service and replayed to `noop`. The enhanced Agent Lab diagnostic preserved the API code, model, path, field, and Zod issue without exposing arbitrary payload values.

### Provider supply silently rejected valid planning demands

After the TreeDX repair, Agent Lab run `20260812t074025557z` scheduled a real four-agent workday and passed content admission. Its first wave and three planning demands were present and valid, but provider polling returned no lease while later idempotent ticks naturally reported zero newly compiled demands. Live read-back found the contract mismatch: every selected agent definition required canonical capability `agent-execution`, while the reconciled provider manifest, availability session, and grants advertised only lower-level capabilities such as `agent_mode_run`. The runner was interrupted and durable workday `workday-agent-lab-20260812t074025557z-guide-steward-profiles-market` was cancelled through `trsd capacity workday-cancel`; no active workday was abandoned. Provider discovery, the agent provider manifest, legacy API class declarations, and reconciled grants now use `agent-execution`. The `agents` seed returned the new capability in its live receipt, provider image/Compose reconciliation converged, and run `20260812t080224335z` immediately admitted two first-wave demands to the configured two `codex-sub` runners while retaining the third as pending. The API also retains a fail-closed pre-persistence invariant for the distinct impossible state where selected participants compile into zero waves.

### Historical timing data under-allocated unrelated agent classes

The first admitted Agent Lab run reused broad `planning` history across unrelated classes and reduced 1,800/2,400-second profiles to 240-second reservations. Provider setup consumed about 35 seconds, leaving Codex about 205 seconds; both agents reached useful content work but expired before durable completion. Demand estimation now learns only from completed assignments with the exact project-agent-class and activity profile, uses the greater of active and elapsed seconds, and otherwise retains the conservative configured profile. Live run `20260812t081303503z` compiled all first-wave demands at 900 seconds.

### Terminal workday cleanup skipped evidence-bearing expired assignments

The timed-out assignments were deliberately retained as `expired` because TreeDX side-effect evidence required operator review, but workday cancellation settled only nonterminal assignments. Their reservations therefore held both grant-concurrency slots until each assignment was cancelled through `trsd capacity assignment-cancel`. Workday terminalization now also releases any still-reserved reservation belonging to a failed, expired, or cancelled assignment, without changing its evidence-bearing lifecycle status. The regression proves one settlement ledger entry, complete reserved-time release, preserved recovery reason, and idempotent replay.

### Agents inferred output categories as signal subject groups

In run `20260812t081303503z`, both first-wave agents created and committed valid TreeDX notes. The evidence agent then supplied editorial/output categories as `subjectGroupIds`; the API correctly rejected them because they were absent from the frozen workday group topology, and the scene failed closed. Signal instructions now state that groups classify the signal subject, must come from exact frozen subject membership, and must otherwise be omitted so the frozen primary-group fallback applies. API diagnostics now return invalid IDs, sorted allowed IDs, and the fallback primary ID. Run `20260812t083702321z` is the clean acceptance retry.

### Concurrent reconcile invocations outlived their CLI output stream

Provider reconciliation output returned before its child apply processes completed, leading two follow-up read-backs to begin duplicate canonical reconciliations against the same Agent image closure. No direct provider mutation was used; all operations converged safely, but the CLI/process supervision boundary needs a concurrency-lock and completion regression. Authoritative read-back ultimately reported both Agent images and provider Compose as `noop`; a final API reconciliation also replayed to `noop`.

The overlapping image preparation also exposed a package-level race: Agent image builds copy the host `dist`, while the destructive build script could run concurrently and remove files during another image snapshot. One apparently healthy image therefore lacked the runtime JavaScript export that its declarations and consumers required. Agent distribution builds now acquire one package-local cross-process lock, recover a dead-owner lock, and always release it. Two simultaneous package builds completed successfully with the runtime export intact; the rebuilt runner image passed an in-container ESM export check, both provider roles resumed healthy polling, and workday `20260812t083702321z` was cancelled before any assignment was admitted.

### Proposal-ready signals lost model identity at the review boundary

Run `20260812t084820962z` completed its three discovery assignments and Guide Steward synthesis with four Zod-valid TreeDX commits, released reservations, exact settlement entries, and the expected `evidence-ready` and `proposal-ready` signals. The following Audience Reviewer assignment nevertheless received the objective fallback instead of the exact proposal. Its immutable graph input contained a string evidence ref and `proposal-ready` signal but no redundant `model` field, so assignment context failed to infer proposal identity; the Agent editorial guard then incorrectly required a Guide chapter for a cross-chapter proposal review. Signal artifact inference now derives `proposal` and `planning_proposal` from the canonical signal type and subject, while explicit chapter scope remains mandatory only for `knowledge` and `page` review. Focused API context, Agent contract, and package build verification pass. Fresh local API, TreeDX, image, and provider read-back converged entirely to `noop` before acceptance retry `20260812t091749188z` began.

### Agent Lab closed on unique-profile coverage while revision work remained active

Retry `20260812t091749188z` live-proved the repaired proposal handoff and completed ten assignments through discovery, synthesis, proposal review/estimation, revision inputs, and technical verification. The planning graph then admitted a final Guide Steward revision using a profile that had already completed once. The Agent Lab controller treated unique profile coverage as terminal, marked the API workday complete, and failed its own scene because the final assignment remained leased. The provider correctly retained that valid lease during completed-workday settlement grace. Agent Lab completion readiness now requires both verified profile coverage and a fully terminal assignment set; a focused regression proves an active later graph assignment blocks completion even when its profile was previously covered. The SDK build passes. A clean full rerun is still required before the scenario is production-ready.

Recovery subsequently failed and settled the final assignment exactly once, but its Codex subprocess remained alive after authority and tool events stopped. Canonical provider reconciliation replaced the runtime container and cleared the process; it was not killed through a direct Docker mutation. The provider now observes authoritative assignment state during execution and propagates revocation through AgentKernel into the Codex abort signal. Focused tests prove both timeout and external-authority cancellation join the active turn, and the Agent package builds. This path remains implemented-but-not-live-proven until an isolated forced-terminalization run demonstrates prompt process exit and no late tool events.

### Proposal revisions started from review evidence instead of the proposal version

Clean retry `20260812t095652588z` live-proved the completion-readiness repair: the workday remained running after ten completed/released assignments and admitted its eleventh dynamic Guide Steward revision. That revision was incorrectly materialized from a review-note/base commit rather than proposal version `5542c452c1887f2f12714b5690fa125efa992b78`, so Codex could not see the existing proposal and attempted to recreate its identity. The governance layer rejected the conflict before commit, the assignment failed/released, and the parent failed closed.

Retry `20260812t104104601z` completed and released ten assignments, exercised mutation-atomic Zod feedback on missing proposal `motivation` and invalid `status`, and again kept the parent open for the eleventh revision. Live decision-input read-back showed the remaining contract nuance: the Guide Steward revision profile is objective-scoped, while exact proposal version `ad698d6bb3045806193f2fa893f03efeda16ce1d` is carried in immutable `upstreamEvidence` signal payloads and proposal evidence refs rather than `relatedArtifact`. The resolver now extracts exact 40-hex proposal versions from either direct proposal artifacts or proposal-routed signal evidence, rejects conflicting versions, and only then falls back to sequential artifact commits or the configured base ref. The focused regression passes 4/4 and the API builds. A fully reconciled live retry is still required.

### Long discovery turns outlived their issued provider access token

Retry `20260812t111459232z` was cancelled through `trsd capacity workday-cancel` after its Technical Verifier could read and validate content but every model-aware create returned `provider_access_token_required`. The assignment had been admitted with valid provider authority, but `runProviderAssignment` copied the connection's originally issued token into the Codex tool runtime rather than refreshing it at assignment execution time. The missing planning-note receipt triggered the correct correction turn; that turn then timed out, expired, and was classified as operator-action because side-effect evidence existed. The Codex subprocess exited, two completed reservations settled, and the expired reservation remains under terminal recovery observation. Assignment execution now resolves `accessTokenProvider` immediately before preparing the kernel/tool runtime and fails closed on an empty refresh. Focused Agent assignment lifecycle tests pass 9/9 and the package builds. Live reconciliation and retry remain required.

### Update-only content revisions committed but produced no durable artifact receipt

Clean retry `20260812t114256347z` live-proved both preceding repairs: its final revision TreeDX handle used exact upstream proposal commit `dfc75c1eb179a8ed6a5f115f2d3d3885a851b3d5`, and the Technical Verifier created its Zod-valid note with freshly resolved provider authority. Ten assignments completed normally. The eleventh Guide Steward assignment then updated the existing proposal, passed model-aware Zod validation, committed `712a574e96bbf76e5997a6dba6309de9188faf15`, and published `proposal-revised`. Receipt construction nevertheless only emitted `treedx_content_receipt` for `content_created`, even though the completion gate already accepted both create and update events. The runtime therefore launched an unnecessary correction turn and expired at the assignment's original deadline; the workday settled all 11 reservations and ledger entries exactly once but correctly failed acceptance with 10 completed and one expired assignment. The canonical receipt builder now composes and deduplicates both creation and update events, requires a commit event, and carries the latest relation plus commit provenance. Focused Agent receipt, content, and correction coverage passes 36/36 and the package builds. During that verification, a knowledge-creation contract exposed that the SDK registry omitted fields required by its own portable knowledge/book Zod schemas; those writable field bindings are now aligned and focused SDK content validation passes 28/28. A reconciled full retry remains required.

### Historical p90 ended an otherwise valid assignment at its exact cutoff

Agent Lab run `20260812t124044202z` reached a Zod-valid Audience Reviewer content commit but expired before it could publish the required signal. The estimator had treated historical p90 as the reservation itself, leaving no closeout or lifecycle margin. Comparable completed history now reserves the greater of p90 plus 60 seconds or p90 multiplied by 1.2, capped by the selected activity profile maximum. Agent Lab lifecycle evaluation also treats a required `expired` profile as a terminal failure instead of waiting indefinitely for success that can no longer occur. Focused API estimation and SDK lifecycle regressions pass; a production-shaped retry remains required.

### Assignment time awareness and resumable closeout became universal

The existing content prompt carried a deadline snapshot and `treeseed.status` could read assignment state, but generic Codex assignments did not always receive a start time, project tool lists could omit the status tool, and closeout instructions did not define a durable continuation contract. Every assignment catalog now includes the read-only status tool. The tool returns authoritative wall-clock `now`, start, deadline, allocated and remaining seconds, closeout threshold, phase, and `shouldCloseOut`. Activity profiles accept a Zod-validated positive `closeoutWarningSeconds`; admission bounds it to at most 20 percent of the reservation with a one-second minimum. All Codex prompts include start, deadline, threshold, live-check cadence, mandatory Zod-valid content or verified source checkpointing, exact resume evidence, and an optional governed extension proposal that cannot change its own deadline or approve itself. Agent focused tests pass 17/17, SDK schema tests pass 3/3, API estimation tests pass 7/7, all three package builds pass, file architecture passes, and the strengthened planned guarantee validates. Live Agent Lab evidence is still required before activation.

### Browser mutations and Atlas live revisions caused hidden observability failures

The first workspace-focus scene passed its visual assertions while the network evidence recorded `403` responses from authenticated view-state updates and a rapid sequence of aborted EventSource requests. The shared browser request helper omitted the Admin proxy's required CSRF header for unsafe direct JSON requests. It now reads the signed-in `ts_csrf` cookie and supplies `x-treeseed-csrf` unless the caller explicitly provides one; the regression proves a direct PATCH carries the token. Separately, Atlas live projection revisions included the advancing observation clock, so unchanged durable state appeared different on every SSE poll and forced stream teardown/reconnect. Live revisions now hash durable source state and cursor positions while historical revisions retain the exact observation instant. Focused UI/API tests pass, and the clean scene rerun above contains no network or console errors.

### Active-team selection could not activate the only available team

The first visual acceptance run found that the team selector preselected the only option even though no active-team cookie existed. Selecting that already-selected option emitted no change, leaving Agent Lab without a team scope. The shell now renders a disabled selected `Choose a team` placeholder until authority is actually active. The Admin team-management contract passes 7/7 and the acceptance scene proves the team becomes active before Atlas loads.

### Repository-backed Designer acceptance exposed unbounded Agent Lab reads

The real TreeSeed team proved the new Designer against live TreeDX definitions rather than a fabricated fixture. It also exposed two API defects: repository-only agents were omitted until their durable runtime class existed, and concurrent Build/detail/overview refreshes repeatedly loaded the same portfolio definition inventory and snapshots. An agent detail without an assignment additionally matched every event whose assignment ID was empty, producing a thousands-row timeline. Repository-only agents now remain visible, definition inventories are bounded, ref-aware, in-flight deduplicated, and invalidated after authoring, runtime-agent definition lookup is project-scoped, concurrent endpoint snapshots share one two-second live observation, and detail timelines require real agent/assignment identity and expose at most the latest 200 entries while retaining total counts and queryable forensic authority. Focused API regressions pass 7/7 and API build/architecture checks pass. Live UI proof opened the repository-backed Architect definition, showed the reusable Designer with no Hands/anatomy controls, opened its supported team-simulation setup, transitioned to focused Simulation Bay, returned to the exact Designer URL, and preserved then cleanly reverted an uncommitted field edit. No repository commit or simulation launch was performed during this interaction proof. The local process previously reached the 4 GB Node heap before these repairs; sustained memory acceptance after a clean restart remains required.

### Repository-authored simulation reached the real team workday boundary

Simulation Bay committed its generated scene and seed through TreeDX and published exact GitHub content refs `aa5c57e0388a5c33b745dcb4fb6ff44b8b54a5d5` and `419a9769411981af0f64f85f8b3043c95cc81913`. This acceptance exposed four recovery defects instead of hiding them behind fixture aliases: remote fetch read-back checked a ref TreeDX does not expose; a fetched content ref could be current while graph/search state remained stale and the reconciler incorrectly planned `noop`; an immutable operation bundle was incorrectly treated as the live provider-runtime root; and the generated simulation invented a fixed local provider identity rather than resolving the active project grant's seed identity and execution-provider inventory. Fresh branch-ref verification, stale-index update planning, explicit `TREESEED_TENANT_ROOT`, and grant-derived provider references now own those boundaries. The resulting operation created and ticked a retained cooperative-planning workday through the reconciled agent runtime.

That live workday then exposed two content/forensic edge cases. SSE projection passed `null` URL filters into the Zod query contract and crashed the API; absent filters are now normalized before validation with a focused regression. The draft generator also selected the alphabetically first historical test even when its target agent was intentionally dormant. It now authors a Zod-validated project-inventory agent test from enabled durable agents and pins that test in the scene. A mixed create/update bundle additionally proved TreeDX batch reads fail atomically when one requested path is missing; authoring now observes each proposed path independently before building one atomic changeset. API lint/build and the focused workday-activity regression pass. The final inventory-test publication and completed Atlas return remain the current live gate; failed operations and reports are retained as forensic evidence.

### Long-running platform operations could outlive a single startup lease

Repository-authored simulations remained `running` after their original operations-runner process disappeared because the operation lease was renewed only once at startup and claim recovery considered expired `leased` records but not expired `running` records. The SDK and PostgreSQL operation stores now reclaim either expired state, record the previous owner and status in `runner.lease_reclaimed`, and fence each execution with a process-unique runner identity while preserving the stable configured provider identity. The shared execution loop continuously renews the lease, serializes renewal attempts, stops the heartbeat before terminal settlement, and fails closed if ownership cannot be renewed. Focused SDK operation lifecycle coverage passes 11/11, the API expired-running recovery regression passes, and both packages build.

Live restart proof reclaimed operations `fbf8bde2-...` and `691f23e9-...` under the new process-fenced runner and terminalized them with their honest underlying errors (a historical legacy provider identity and a duplicate retained workday, respectively) instead of leaving permanent running leases. A fresh simulation plan resolves the canonical `capacity-provider:treeseed/agents` profile without diagnostics. The next live gate is a short new governed simulation using that canonical inventory, not replaying either historical failed operation.

### Local disk pressure is now bounded before Docker image work

The local provider and image path observes filesystem capacity before each Docker build, reserves the greater of 4 GiB, ten percent of the filesystem, or the configured minimum, and adds at least 8 GiB of build headroom (or twice the build-context size). Under pressure it uses the provider-owned Docker adapter to prune build cache and re-observes capacity before allowing the build; failed and successful builds also perform bounded cache maintenance. The provider availability snapshot publishes the same disk-capacity constraint so assignment admission can fail before a runner exhausts the host.

Current read-back reports 53 GiB free (57 percent filesystem use). Agent Lab scene evidence occupies 95 MiB; the operations-runner state occupies 4.4 MiB. Docker reports 7.8 GiB reclaimable build cache and 1.2 GiB reclaimable inactive volumes. `trsd clean --plan --json` reports 1.25 GB of project artifacts, dominated by the regenerable Agent Docker context, with no unfinished-run blockers. The new acceptance evidence is intentionally retained until its receipts are recorded; project cleanup must remain a deliberate `trsd clean` operation and must not stop the watched local platform mid-run.

### Short simulation planning now uses the production scheduling compiler

The repository-authored simulation draft previously fixed every run at 900 seconds, three rounds, 120-second assignments, four concurrent slots, and every enabled agent. The SDK and CLI now carry an explicit bounded selection, duration, planning-round count, assignment timebox, and maximum concurrency into the API draft. The API filters the repository-backed agents, resolves their exact signal DAG through the canonical workday graph policy, and invokes the same cooperative-planning compiler used immediately before scheduling. Invalid subsets and insufficient agent time now fail during `--plan`, before TreeDX publication or a platform operation exists. Focused API coverage passes 3/3, the CLI simulation contract passes, and SDK/API/CLI builds pass.

Live planning found the smallest valid current chain to be Evidence Researcher → Guide Steward → Audience Reviewer. An underfunded 600-second plan was rejected with exact 1,440 required versus 1,080 allocated planning seconds. The accepted 900-second plan published immutable content commit `633f70f0d2b10f7b8c2d5b36660ba6a080431ae3`, created operation `6404d191-0ea5-4807-8203-9cbca89ccffa`, and started the real Codex-backed workday `agent-lab:6404d191-0ea5-4807-8203-9cbca89ccffa:cooperative-planning-demo`. Its first 180-second assignment reached AgentKernel, TreeDX context, live time-left checks, and Codex execution, then returned cleanly because the execution exceeded its 121,420 ms usable timeout. The lease and runner were released with no zombie process or duplicate attempt.

That failure exposed two observability/lifecycle defects. `workday-log --follow` aborted on a transient `fetch failed`; it now retries only bounded transient transport failures, preserves its activity cursor, emits no duplicate or non-event JSONL records, and still fails immediately on authorization or contract errors. A focused regression passes 2/2 and repeated live follow windows remained connected. A returned required profile also remained visually active after its immutable retry deadline, even though no second lease was possible. Agent Lab lifecycle evaluation now treats that exact post-deadline state as a terminal profile failure while retaining returned-as-retryable behavior before the deadline. Focused SDK Agent Lab coverage passes 24/24. The retained pre-repair run closed honestly at its original duration bound: parent and envelope failed, assignment totals are one failed with zero pending or leased, the 180-second reservation released in full, and exactly one usage actual plus one settlement ledger entry remain. A fresh post-reconcile run is required to prove prompt closure and successful three-profile completion.

The first 300-second retry then exposed a distinct Codex transport boundary. Evidence Researcher created and validated a dossier plus linked question, committed `d78565ba4af877392bba32ab7488d62ea1bb686a`, published its required signal, and emitted `turn.completed`, but the streamed SDK iterator remained open until the wall-clock timeout. The provider consequently converted completed durable work into a retryable failure. Streamed execution now treats the authoritative `turn.completed` event as terminal, returns its buffered result and usage immediately, and closes the iterator instead of waiting for transport teardown. Timeout and authority-revocation cancellation remain fail-closed. Focused Agent coverage passes 8/8, the Agent package builds, and file/architecture policy passes.

After canonical Agent image build and provider reconciliation, operation `d33c8fb7-18b1-475d-9044-4ebf36277876` live-proved the repair. Evidence Researcher created, linked, validated, and committed the objective dossier at `0e3bcfda4a3380bcfca8098bee069f26d667b8fd`, published `evidence-ready`, emitted `turn.completed`, and immediately reached `completed/released`; TreeDX workspace close finished normally and no timeout was recorded. Audience Reviewer was admitted from the resulting signal as the next wave. The complete three-profile workday, settlement, focused Atlas, and Designer return remain in progress.

## Execution Log

| Date | Checkpoint | Evidence | Result / next gate |
| --- | --- | --- | --- |
| 2026-08-11 | Live portfolio established | 31 `treeseed-ai` repositories with branch and visibility read-back | Seeds now reconcile existing GitHub authority rather than hypothetical repository names. |
| 2026-08-11 | Platform workset federation proven | 13 detached repositories, no `.gitmodules`, repeated 13-action `noop` | Make this the normal local entry point, then retire Market-root compatibility gitlinks. |
| 2026-08-11 | Receipt federation proven | 14-repository receipts with fresh remote proof; stage lifecycle scenarios pass | Perform the first reviewed real staging promotion from a full receipt after non-hosted gates settle. |
| 2026-08-11 | Singleton gateway contract proven | Private clean clone, descriptor pin, HTTP/cookie/SSE/WebSocket tests | Continue route and persistence extraction; hosted proof remains suspended. |
| 2026-08-11 | First Market route extracted | `/v1/market/profile`; Admin 578 routes / 149 Market; private staging `9193fbd809f11708914475cb52bffd6147782e74`; six tests | Extract the next cohesive Market route/persistence family without increasing cross-plane coupling. |
| 2026-08-11 | Governance execution authority linked to save receipts | Stable checkpoint authority, clean repository boundary, ancestry-filtered embedding, tamper rejection; SDK focused 5/5 and lifecycle 14/14 | Canonical dependency compilation and freshness work continued in the next checkpoint. |
| 2026-08-11 | Cross-project governance freshness enforced | Proposal hashes include normalized dependencies; accepted decisions snapshot current same-team decisions; API compilation rejects stale/forged authority; checkpoint trailers require matching receipts; later governed-path changes fail; stage validates through the resolved control plane and hashes/persists the evidence; API graph 10/10, governance 6/6, SDK lifecycle 16/16 | Run a real authenticated control-plane staging acceptance after the non-hosted portfolio candidate settles. |
| 2026-08-11 | Unified workday boundary corrected | User review rejected the invented direct “start work” and mixed governed/ungoverned final lanes. Current Codex edits are acknowledged as transitional; team inventory and project-local workday authority are the required final path. SDK 1,398/1,398 unit tests passed; 56/58 lifecycle tests passed in the full run and both recovery regressions passed after the merge-base repair; focused API 52/52 and CLI 22/22 passed. | Replace portfolio-file worksets, integrate Codex with planning/acting assignments, require authority coverage for every changed project, then rerun full verification before any save or stage. |
| 2026-08-11 | Live team-inventory and Codex-default cutover | Authorized project-inventory API; exact-ref CLI workset; 13 live Platform entries; legacy templates archived without deletion; `codex-sub` rendered as default; agent seed recovered after paginated history fix | Bind this inventory and provider to a real planning/acting assignment, then make save/stage fail closed on complete per-project authority. |
| 2026-08-11 | Workset save authority made fail-closed | Federated save compares each workset repository to its exact inventory commit and rejects non-generated source paths without matching project/base authority; governed save/stage and ungoverned rejection scenarios pass | Extend the same exact-base contract to the Platform root, then dogfood a real multi-project workday. |
| 2026-08-11 | Shared capacity allocation converged | Codex is the rendered default; agent and platform seeds returned `seed-allocation-4f931c26d45d9a413f6df5ca` across four alternating live applies; portfolio seed replay is 29/29 unchanged | Run the first real planning and acting workday against this provider/allocation pair. |
| 2026-08-11 | Codex default moved into assignment supply selection | Provider availability marks the manifest default; shared SDK ranking and API demand selection choose `codex-sub` for equal eligible supply while explicit team preference wins; live open session `75512420-f77d-4f1d-b0fb-af63b2ce639c` at sequence 59 advertises only `codex-sub` as preferred | Run the first Platform planning workday, then prove the selected assignment and execution-run provider IDs. |
| 2026-08-11 | First Zod-validated internal agent became schedulable | CLI/API/SDK share the portable agent schema; TreeDX exact commit `b0886e6f88efd85db5f491c73f93b138d1e05800`; active planning-only class read back through CLI; identical replay returned zero changes | Preserve/publish the pending content receipt, then run and inspect the first real Codex-backed planning workday. |
| 2026-08-12 | Model-aware content validation became mutation-atomic | SDK model schemas, Agent preflight/read-back validation, field-addressable live Codex diagnostics, focused SDK 13/13 and Agent contract 2/2 | Consolidate Core/Astro, remaining models, TreeDX ingestion, Admin chat, and CI onto the portable registry. |
| 2026-08-12 | Portable proposal-type validation aligned | SDK contract validation, SDK content Zod, submission readiness, and Core/Astro use one kebab-case identifier rule; SDK content 13/13 and Core content 2/2 passed | Move the remaining Core/Astro fields, template/workday models, TreeDX ingestion, Admin chat, and CI to the portable registry. |
| 2026-08-12 | First internal self-hosting planning loop completed | Assignment `assignment_XMmAGWSq3szOvTLt43U1kdNcbw--4smL`; TreeDX commit `55e0e4aa6667407b0383665a5db6d10b59b6e800`; durable open proposal; usage actuals; consumed reservation; actual-settlement ledger entry; released lease/counter | Extend the governed path to a reviewed decision, acting assignment, source checkpoint, and multi-project federation receipt. |
| 2026-08-12 | Local workday scheduler restored and live-proven | Default desired-state graph includes the API-owned operations runner; focused SDK 27/27 and CLI dev 8/8 passed; live runner readiness succeeded; workday run `06bfa0b7-8df4-4a7c-9410-64949f14a63b` closed with one completed assignment and one succeeded mode run | Repeat closure, then run multi-agent and multi-project workdays before declaring the capacity lifecycle production-ready. |
| 2026-08-12 | Private content reconciliation converged | API-owned central-authority verification and sealed one-use credential preparation; SDK forwards only an opaque delivery ID; interrupted/concurrent retry coverage; live `market-api-content` sync; 15-project TreeDX replay entirely `noop` | Repair targeted dev dependency selection, then model publication/resumption for unpublished TreeDX authoring receipts. |
| 2026-08-12 | Every current content collection gained a portable Zod boundary | SDK registry covers all agent-mutable models plus agent tests, workday summaries, and template products; Core consumes the three auxiliary schemas; live frontmatter and exact-field error tests pass | Route TreeDX ingestion, Admin chat feedback, and CI through the same registry, then retire overlapping Core declarations. |
| 2026-08-12 | Discussion chat adopted the portable content registry | API validates generated Discussion/message/event sources before TreeDX staging and validates read-back before Admin/chat projection; invalid write/read tests preserve exact fields; local API apply passed and replayed `noop` | Render structured diagnostics in Admin chat and extend ingestion/CI validation across all model collections. |
| 2026-08-12 | Content diagnostics reached chat and portfolio CI | Shared Discussion UI preserves failed optimistic messages and renders exact diagnostic fields; SDK publication admission validates known models before runtime/R2 work; stable JSON errors; 644 live documents passed; focused SDK/API/UI tests and builds passed | Retire overlapping Core/Astro declarations and apply the registry to remaining non-Discussion TreeDX ingestion boundaries. |
| 2026-08-12 | Generic TreeDX content ingestion became model-valid | SDK backend validates first-party reads and pre-mutation writes, structures malformed frontmatter, preserves invalid-vs-missing semantics, and fixes direct paths derived from `**`; focused SDK TreeDX/publication 29/29 | Converge bespoke API TreeDX readers and overlapping Core/Astro declarations on the same registry. |
| 2026-08-12 | Portable content admission became the cross-runtime base contract | Core validates raw frontmatter before Astro adaptation; API workday demand, planning artifacts, knowledge parsers, and governance content changesets validate before use/mutation; knowledge `bookId` graph binding and canonical `knowledge-gap` repaired; 646 documents valid | Finish the smaller Agent Lab/contextual-reader tail and register end-to-end verifier evidence. |
| 2026-08-12 | Agent Lab and relation search stopped hiding malformed content | Inbox reads, answer writes, and indexed relation projection use SDK-owned Zod models; invalid documents become field-addressed actionable issues; answer submission closes the workspace before any changeset; focused API 5/5 plus package build and architecture checks passed | Prove the diagnostics through a real Agent Lab workday and add end-to-end verifier evidence before activating the guarantee. |
| 2026-08-12 | TreeDX preserved empty YAML sequences | Exact `relatedQuestions: []` controller regression, TreeDX 5/5, rebuilt local unit, repeated reconcile `noop`; live Agent Lab displayed exact model/path/field diagnostics before the repair | Continue the same production-shaped run through assignment and artifact completion. |
| 2026-08-12 | Portable Zod coverage became a registry invariant | Every built-in content-backed model must report `portable: true`; malformed agent profiles expose exact nested fields; SDK focused schema suite 5/5 | Keep this invariant in package CI and complete real Agent Lab evidence. |
| 2026-08-12 | Agent Lab exposed provider capability drift | Live graph contained one running wave and three pending demands; provider supply omitted required `agent-execution`; durable workday safely cancelled; canonical manifest/runtime/API names aligned; live seed receipt and fresh run admitted two demands to `codex-sub`; focused Agent 1/1 and API 9/9 | Follow run `20260812t080224335z` through TreeDX artifacts, Zod validation, usage, and settlement, then make empty supply-selection diagnostics operator-visible. |
| 2026-08-12 | Live Zod feedback and realistic agent budgets proven | Invalid note/question status values were rejected with exact model fields before TreeDX mutation and corrected by Codex; exact-class completed history now yields 900-second demands; SDK schema/content suite 30/30, API focused 4/4, Agent focused 3/3, and all three package builds pass | Complete retry `20260812t083702321z` through four-agent artifact, signal, usage, settlement, and workday closure evidence. |
| 2026-08-12 | Terminal reservation recovery became workday-owned | Operator cleanup released two stranded 240-second reservations; terminalization regression preserves expired evidence while settling once; subsequent failed run released both 900-second reservations; local API and Agent provider plans replayed `noop` | Prove automatic cleanup on a future interrupted workday and add reconcile child-process completion/concurrency coverage. |
| 2026-08-12 | Agent distribution builds became concurrency-safe | A raced image lacked `resolveAgentRuntimeProviders`; no assignment was admitted; package-local build lock added; simultaneous builds passed; rebuilt image export verified in-container; provider and API plans replayed `noop` | Complete clean acceptance run `20260812t084820962z`, then add CLI child-process completion coverage. |
| 2026-08-12 | Four-agent discovery and synthesis completed before exact proposal handoff failed | Run `20260812t084820962z`; four completed/released assignments; four TreeDX commits; required evidence/proposal signals; exact usage and settlement; evaluation failed closed before content mutation | Derive proposal identity from immutable signal inputs, retain chapter scope only for page/knowledge review, and rerun the complete scenario. |
| 2026-08-12 | Proposal evaluation handoff repaired and live retry admitted | Focused API context 3/3; focused Agent proposal-review contract 1/1; API and Agent builds; in-container runner export; all 11 local provider dependencies `noop`; run `20260812t091749188z` admitted real Codex-backed discovery assignments | Follow every wave through Zod-valid artifacts, signals, usage, settlement, and durable workday closure. |
| 2026-08-12 | Dynamic revision work stopped being hidden by profile coverage | Run `20260812t091749188z` completed/released 10 assignments and admitted a final revision; parent closed early; focused SDK regression rejects completion with any nonterminal assignment; SDK build passes | Let the preserved lease settle, reconcile the SDK closure, and rerun through a quiescent terminal graph. |
| 2026-08-12 | Proposal revision base authority corrected across artifact and signal shapes | Runs `20260812t095652588z` and `20260812t104104601z` stayed open through the eleventh dynamic assignment; latter completed/released the preceding ten and live-proved exact Zod repair; objective-scoped revision exposed proposal version `ad698d6bb3045806193f2fa893f03efeda16ce1d` inside immutable signal evidence; API regression 4/4 and build pass | Reconcile API, prove final revision starts at the exact signal-routed proposal version, and complete the full workday. |
| 2026-08-12 | Assignment tool authority refresh added | Run `20260812t111459232z` exposed `provider_access_token_required` after long discovery; missing receipt failed closed; recovery turn timed out; workday cancelled canonically; Agent lifecycle 9/9 and build pass | Reconcile Agent images, verify terminal reservation recovery, and rerun with a fresh token per assignment. |
| 2026-08-12 | Update-only TreeDX revisions gained durable receipts | Run `20260812t114256347z` used proposal base `dfc75c1e...`, committed validated revision `712a574e...`, and published its signal, but legacy create-only receipt collection caused false correction and expiry; all 11 reservations/ledger entries still settled once; focused Agent 36/36 and SDK 28/28 pass; canonical image/Compose reconciliation verified the new closure and a fresh four-unit provider plan is entirely `noop` | Rerun through 11 completed/released assignments and successful scene closure. |
| 2026-08-12 | Assignment deadlines gained live status and resumable closeout | Universal `treeseed.status`; start/deadline/remaining/phase contract; bounded Zod-valid warning; content/source checkpoint and extension-proposal instructions; Agent 17/17, SDK 3/3, API 7/7, package builds and architecture gates pass; local live reconciliation smoke 6/6 `noop` | Rebuild reconciled provider images, run a bounded Guide Agent Lab profile, and inspect timing calls, durable receipts, settlement, and zero-residue closure. |
| 2026-08-12 | Integrated Agent Lab focus foundation accepted locally | Shared UI focus/overlay primitives and cross-island URL/history authority; authenticated vital density; focused Atlas and deterministic Escape; protected diagnostic evidence; SDK Zod forensic contracts; API/Agent/CLI machine surfaces; scene `20260812t164305372z` passed with zero console/network errors | Prove real-shell browser Back and designer/editor draft preservation, complete real simulation acceptance, responsive/accessibility coverage, historical replay retention, and only then retire the external viewer. |
| 2026-08-12 | GitHub core reconciliation lifecycle live-proven | Smoke `20260812164555`; pre-cleanup `20260812165154`; acceptance `20260812165207` passed 11/11 against `treeseed-ai/market`; final cleanup `20260812165226` was 11/11 `noop`; focused tests 3/3, SDK build and architecture gates pass | Replay visibility/licensing and lifecycle policy across all 31 repositories without deletion, then require a portfolio-wide `noop`. |
| 2026-08-12 | Agent Lab Back and draft-state contract live-proven | One UI-owned surface-mode hook; Designer/source/rich editor mounted-draft regressions 11/11; typed scene browser-history action and controlled-SSE classification; real scene `20260812t170331796z` passed 12/12 with zero console/network errors | Add overlay-exit unsaved confirmation, simulation return-state proof, responsive/accessibility matrix, and exact historical replay before viewer retirement. |
| 2026-08-12 | Repository-backed Designer and simulation return live-proven | Real TreeDX Architect definition; reusable Designer; supported team-simulation setup; focused Simulation Bay; exact Designer return URL; draft preserved and reverted; no Hands/anatomy; API projection regressions 7/7 and build pass | Restart cleanly and sustain live polling below memory bounds, add repeatable scene selectors, then launch a real validated simulation and prove Atlas return evidence. |
| 2026-08-12 | Repository-authored simulation reached a real retained workday | TreeDX/GitHub commits `aa5c57e...` and `419a976...`; canonical provider `capacity-provider:treeseed/agents`; workday `agent-lab:27ab4e43-e1ca-4d39-b3c2-e2684aa7b755:cooperative-planning-demo`; stale-index, runtime-root, Zod null-filter, provider-reference, test-selection, and mixed-bundle recovery fixes | Publish the generated project-inventory agent test, rerun through completed assignments and settlement, prove automatic focused Atlas navigation and exact Return to Designer, then record responsive/accessibility evidence. |
| 2026-08-13 | Platform-operation leases became renewable and process-fenced | SDK operation lifecycle 11/11; API expired-running recovery regression; SDK/API builds; live operations `fbf8bde2-...` and `691f23e9-...` reclaimed and honestly terminalized under a process-unique runner identity | Launch one new short governed simulation against `capacity-provider:treeseed/agents` and prove operation, workday, assignment, usage, settlement, focused Atlas, and Designer return all close cleanly. |
| 2026-08-13 | Agent Lab responsive replay circuit passed | Desktop `20260813t160259380z`, tablet `20260813t160214573z`, and mobile `20260813t160043223z` each passed 19/19 with zero console/network errors; mobile audit `20260813t155-9c58f1d8` produced zero findings | Add nested overlay/unsaved-exit, zoom, forced-color, immutable historical-definition, and real simulation return evidence before activating the guarantee or retiring the external viewer. |
| 2026-08-13 | Local disk headroom and evidence footprint verified | 53 GiB free; 95 MiB scene evidence; 4.4 MiB runner state; Docker build path reserves filesystem and operation headroom and performs bounded adapter-owned cache pruning; clean plan found no unfinished-run blockers | Preserve current evidence until receipts are recorded; use deliberate `trsd clean` between campaigns and keep all image builds behind the disk-capacity gate. |
| 2026-08-13 | Short simulation preflight and follow recovery implemented | Exact three-profile DAG; invalid graph and 1,440/1,080 budget rejection before mutation; live commit `633f70f...`, operation `6404d191-...`, and real returned Codex assignment; API 3/3, CLI follow 2/2, SDK Agent Lab 24/24; package builds | Let the retained pre-repair operation close, reconcile the lifecycle change, then run a feasible three-profile workday through artifacts, signals, usage, settlement, focused Atlas, and Return to Designer. |
| 2026-08-13 | Completed Codex turns no longer become transport timeouts | Pre-fix durable commit `d78565ba...` exposed the open-stream race; Agent 8/8 plus build/architecture pass; canonical provider rebuild/up; live operation `d33c8fb7-...` completed/released Evidence Researcher at commit `0e3bcfda...` and admitted Audience Reviewer | Follow the remaining graph through terminal settlement, then exercise the completed run in focused Atlas and Return to Designer. |
| 2026-08-13 | Self-development transition integrity slice implemented and live-challenged | SDK-owned operator registry and machine discovery; complete capacity-plan CLI lifecycle; API idempotency; assignment-scoped exact-ref workset authority; admission-fenced terminalization; immutable proposal revisions; bounded workday evidence; supervisor pause/stop and repository drift status; historical Designer/Atlas scene `20260813t184201842z` passed. Real Guide Steward run `20260813t184812984z` closed the envelope `failed` and parent `degraded` with 4 terminal assignments (3 completed, 1 failed), 4 ledger entries, complete cleanup, and no hidden success after `proposal_output_type_invalid`. | Repair the agent output-type/content contract exposed by the run, rerun the Guide Steward profile to green, then execute the governed SDK/CLI source canary. Platform-root pointer authority and interruption-safe unpublished TreeDX receipt journaling remain release blockers; do not run `trsd save` or stage until they are implemented and proven. |
| 2026-08-13 | Shared context-query readiness became an assignment gate | Guide Steward references exact query set `guide-work-foundation@2`. Authoring commit `0fe46c96...` added direct tests for both exact member queries and was pushed/read back from the content staging ref. The index returns 1 item and 74/1,000 tokens; the section check returns 3 items and 3,322/4,000 tokens; the composed set returns all 4 exact pages and 3,396/5,000 tokens. Latency is diagnostic, not a readiness gate. The control plane persists only status, semantic assertions, aggregate stats, digest, definition revision, observed source ref, and freshness; it never persists result content, and the provider executes selected queries again against current TreeDX data for assignment context. | Extend the same exact tested context inventory to every selected editorial profile. Do not run Codex until each selected profile resolves only fresh passing revisions. This is context readiness, not agent readiness or a Guide repository outcome. |
| 2026-08-13 | Canonical agent outcome catalog established | Twelve dependency-ordered v2 capabilities now define readiness from dynamic context through six profiles, exact handoff, recovery, source golden, and Guide golden. Structured verifier results require exact identities, authoritative read-back, verified repository postconditions, evidence paths, and zero cleanup; three consecutive same-generation variants are required and source or verifier changes invalidate the streak. Earlier runs `2026-08-14T03-16-26-080Z`, `2026-08-14T03-17-05-982Z`, and `2026-08-14T03-17-42-609Z` proved the dynamic-context workflow at TreeDX ref `e49d494...`. The completed proof-harness generation `7a20a13b...` intentionally invalidates that older streak. CLI status now reports all 12 capabilities blocked and exposes, per capability, the exact CLI commands, named semantic predicates, minimum repository read-backs, and candidate invocation required to begin new live evidence. | Rerun dynamic context three times on the completed generation, then test each dependency in order using an exact `treeseed.agent-guarantee-proof/v1` input. Do not call TreeSeed agent development operational until `agent.system.guide-golden` is active. |

## Ordered Next Work

1. Activate `agent.context.dynamic-readiness`: finish the editorial context inventory, add semantic tests, run every query without agents, and prove three same-generation variants while keeping result content ephemeral.
2. Implement and prove the remaining profile and handoff verifiers in catalog dependency order; every verifier must assert semantic artifacts and forbidden authority, never generic completion or artifact counts.
3. Run the bounded CLI-driven source golden workflow before any Guide canary, including independent review, deficient-checkpoint rejection/revision, exact local/remote read-back, settlement, and zero residue.
4. Run the Guide golden workflow twice in ephemeral projects and once against canonical Guide content, including interruption/resume; operational status begins only after the canonical semantic target-ref outcome passes.
5. Make staging reject every changed project without complete project-local authority, but keep staging disabled during this catalog campaign.
6. Run a real multi-project dogfood initiative through the local TreeSeed team using Codex as the default provider, including dependency discovery that returns newly affected projects to planning.
7. Establish the private Market repository as the canonical proprietary source workspace, then extract Market API ownership and persistence from Admin until the Admin descriptor reports zero Market routes; preserve exact route ownership and gateway coverage during the move.
8. Complete full managed control-plane reconciliation and sovereign traffic/data-separation acceptance, including explicit provider-protocol URL separation and the journaled migration command.
9. Complete TreeDX/R2 content authority project by project, then remove software content workflows and paths only through verified cutover receipts.
10. Replay the live-proven GitHub lifecycle and policy checks across the 31-repository portfolio, run isolated Cloudflare acceptance with cleanup before and after, and require final `noop` plans. Include a bounded dependency security audit for the generated Market API graph.
11. Execute a reviewed receipt-only staging promotion after all non-hosted guarantees pass; do not use gitlinks as authority.
12. Finish and review OpenTofu topology, then explicitly restore protected singleton hosted staging and run the same gateway suite through `api.treeseed.dev`. Production release remains fail-closed until that review completes.

## Update Discipline

When changing this ledger:

- record exact receipt IDs and live commits, never credentials;
- distinguish implemented code, live provider evidence, and planned work;
- do not mark a phase complete from unit tests alone when live acceptance is required;
- add incidents when recovery exposes a cross-system contract defect;
- keep hosted-deployment suspension visible until it is explicitly lifted by reviewed architecture;
- ensure the Platform filtered snapshot copies the updated ledger and replays to `noop`.
# Agent Lab usability audit — 2026-08-12

The integrated Agent Lab is the immediate development-system gate. A live review found that the root Atlas bootstrap loaded every workday event and mode-run evidence row for the operating day. The current local team contains roughly 11,000 workday events and 10,000 mode runs, so the wide ordered PostgreSQL projection exhausted temporary storage, terminated the API process, and left the browser with an empty or stalled Atlas. The UI then compounded the failure by opening an unpaged event dock by default, hiding its filters in a disclosure, and reducing the missing-topology explanation to a small alert below an otherwise blank canvas.

Canonical ownership for the repair is:

- API owns a bounded portfolio activity window, exact event totals, explicit selected-workday replay, and causal empty/degraded projection alerts.
- SDK owns presentation-neutral activity-window metadata.
- UI owns visible filters, local paging over the bounded live window, meaningful empty-state presentation, and progressive forensic disclosure.
- Admin continues to compose the reusable Atlas and the existing filtered/cursor-paged entity collections; it does not add page-local event clients.

The durable event endpoint, CLI JSON/JSONL, and authorized forensic tools remain the complete evidence surfaces. Bounding the interactive portfolio projection must not discard durable evidence or weaken exact replay for a selected workday. Before longer workday campaigns resume, every `/app/work` route must be inspected in the authenticated collaborative browser and the corrected Atlas must be exercised with a very short workday and a small agent selection.

## Agent Lab human-observability repair — 2026-08-12

The authenticated local application now renders one current Atlas project circuit with 22 agent/profile nodes instead of combining inactive historical topology revisions. The live event dock displays 25 rows per page with search, type, severity, and flow filters. Routine scheduler traffic is hidden by default but can be restored explicitly; live proof showed 375 meaningful events in the latest bounded 500-event window out of an exact 6,470-event team total, and pagination/reset behavior worked. Atlas defaults to a closed dock for new view state and explains genuinely empty topology with workday, assignment, and event context plus direct recovery actions.

Build now presents agent definitions as the primary card collection, paged 24 at a time. Its flow starts at the active project with 17 relevant nodes; optional group expansion remains available and live-proved 108 nodes when requested. The repository-backed Designer opened from the agent overlay, preserved its URL and definition state through focused Expand/Shrink, switched activity profiles, and opened/closed the supported simulation setup without launching a workday. Relationship Search no longer eagerly renders 150 connected records: it opens the map only on request and bounds the visual view to 72 nodes while retaining the complete searchable record collection.

The workday detail page no longer exposes its bounded 50-event response as an unfiltered ordered list. A reusable UI-owned forensic event explorer provides search, type/severity/routine filters, responsive table presentation, and 25-row paging while directing agents to the API/CLI for evidence beyond the current page. All Agent Lab routes—Direction, Results, Find, Agents, Workdays, Assignments, Executions, Artifacts, and System Events—were revisited in the signed-in browser and returned current content after the latest-workday date fallback repair. Focused API projection regressions pass 10/10; SDK, UI, API, and Admin builds pass; UI file-size and architecture policy pass.

Authenticated scene `20260813t002808753z` then passed all 12 focus, vital-density, Escape, and browser-Back steps in the managed local shell with no console or network errors. Remaining acceptance before general workday debugging resumes: add stable browser-scene assertions for the new event and collection defaults, complete responsive inspection (the collaborative browser resize transport timed out during this pass), confirm exact historical replay, then run one short one- or two-agent workday and watch it from Atlas through settlement. Standard `trsd clean` follows only after its forensic evidence has been catalogued.

## Internal workday visibility and lifecycle follow-up — 2026-08-13

Canonical provider reconciliation rebuilt the absent Agent manager and runner images, restored both Agent and platform-operation Compose units, and fresh status now reports all six provider resources ready with no blockers. The first deliberately small planning selections exposed a plan/apply parity defect: CLI plan checked repository binding and class presence but did not compile the selected signal DAG or cooperative time budget, while API execution correctly rejected an invalid single-profile graph and an underfunded two-profile graph. Scheduling validation therefore remains an API-owned preflight gap; the CLI must consume that exact read-only result rather than duplicate the planning compiler.

Bounded acceptance proved a real two-profile chain. Audience Reviewer completed through Codex, committed its assessment through TreeDX, and published the upstream signal that admitted Guide Steward. It also exposed two operator-boundary defects. First, the authenticated browser fixture belongs only to its isolated visual-audit team, so an internal team run is correctly invisible in that session. The supported human path is an installation-supplied pending membership claimed by its verified account; automation must not weaken authorization by silently adding visual-test identities to the development team. Second, `workday-cancel` targets a project envelope while `workday-run` returns a parent coordination-run ID. Cancelling only the envelope left parent admission authority active. The CLI now exposes and documents `workday-run-cancel`, which transitions the API-owned parent and terminalizes remaining envelopes and assignments; its request compiler has focused coverage.

Fresh summary read-back proves the run closed without hidden work: the parent and Market envelope are cancelled, assignments total two with one completed and one failed, pending and leased counts are zero, mode runs are terminal, reservations total two, usage actuals total three, and ledger entries total two. One ledger entry is the normal Audience Reviewer provider settlement and one is explicit terminalization of Guide Steward. The focused CLI suite passes 5/5 and the CLI build passes. A broad standard clean plan was intentionally not applied because it would remove about 1.47 GB of retained scene evidence, including the accepted workspace-focus run; cleanup remains pending until the relevant evidence is bundled or a narrower supported cleanup scope exists.

Current UI observations are therefore honest but split by authority: the isolated visual-audit team shows a clear zero-topology state with explicit recovery actions, filters, and bounded docks, while the completed internal workday is available only to a principal with TreeSeed team membership. The next acceptance gate is to sign in with the seeded owner identity, select TreeSeed, observe a newly preflighted bounded run in Atlas, exercise Designer and workday detail from that same run, and prove responsive behavior. Collaborative-browser resize continues to time out and remains an infrastructure-level evidence blocker rather than a CSS pass.

Atlas now also distinguishes an inaccessible URL-selected workday from an ordinary empty team. The active-team boundary message reveals no foreign workday details, explains that the requested run is not visible to the current team, and provides a clear-selection action. The behavior is live-proven in the isolated browser session against the retained internal run URL; focused UI tests pass 9/9, and UI build plus file-architecture verification pass.

### API-owned workday preflight

Workday plan/apply parity is now owned by one API scheduling compiler. The read-only preflight resolves approved provider membership, project inventory, allocation, active grants, TreeDX bindings, exact repository agent graphs, the cooperative wave DAG, and the time budget before any workday run, envelope, event, planning participant, or wave is persisted. The actual scheduler invokes the same resolver again immediately before mutation, so a successful plan cannot replace execution-time stale-state validation.

The SDK exposes the preflight as a normal Market client method and the CLI `workday-run --plan` now reports its exact facts instead of running a partial repository/class check. Live invalid-graph and insufficient-time plans both failed before creation. A valid Market plan reported six selected activity profiles, five waves, three rounds, 5,100 required agent-seconds, and 5,400 allocated planning seconds. Repository integration asserts zero runs, envelopes, events, participants, and waves after preflight; the authenticated route test also proves `teams:manage:team` authorization and no call to workday creation. Focused API tests, the route-descriptor contract, SDK/API/CLI builds, and managed local reconciliation pass.

### First-agent Designer entry repair

The Build/Designer review found that a project with zero agents could not create its first repository-backed agent because the UI derived project choices from existing agent cards. The API build projection now returns the authorized team project inventory independently, and the reusable flow canvas merges those choices with project identities observed on records. The visual-audit project is now available in the live creation dialog even with zero configured agents.

URL-opened creation also reopened after live projection remounts because closing only changed component state while `?create=agent` remained authoritative. The flow canvas now owns creation URL/history state: toolbar actions add it, Cancel/Close remove only that parameter, and browser history synchronizes the dialog. Live browser proof confirms the project choice and stable close. Focused UI tests pass 4/4, the API projection test passes 3/3, and UI/API builds pass. The isolated visual-audit project correctly remains unable to launch a production-shaped simulation until it has a TreeDX exact ref, enabled repository agents, and an active governed provider grant; the UI exposes those three specific diagnostics rather than fabricating a runnable agent.

Simulation Bay now follows the agreed Easy/Diagnostic boundary. Its normal Build view shows a compact project-profile, workday-scene, and readiness card; the visual-audit fixture reports its three real governance/repository blockers in plain language. The large generated YAML/MDX editors are absent until the operator chooses `Show generated source`, which toggles `aria-expanded` and reveals the same editable definitions without changing save, validation, or launch authority. Live browser inspection proved zero editor grids in Easy mode and one diagnostic editor grid after expansion. The Agent Lab guarantee validates with zero diagnostics, focused UI tests pass 10/10, and managed local reconciliation completed successfully.

### Short human-observability workday

The CLI now exposes the API's existing cooperative-planning controls as `--planning-rounds` and `--assignment-timebox-seconds`. These values are carried in the normal workday request, compiled by the read-only API preflight, and compiled again by execution; they do not bypass the complete signal DAG, provider grants, TreeDX authority, or assignment admission. Focused CLI tests pass 5/5 and the CLI build passes.

Live plan for one round, 120-second assignment timeboxes, two concurrent slots, and the Audience Reviewer/Guide Steward selection reported six complete graph participants, five waves, 720 required agent-seconds, and 1,620 allocated planning seconds. Run `f0b1e3e8-3957-43a2-b370-93636f957318` then started through the API-owned path. Initial read-back shows Audience Reviewer leased to a real reconciled runner through `codex-sub`, with a TreeDX assignment workspace, exact context-layer digests, bounded time authority, and an active forensic mode run. Final Atlas, artifact, signal, usage, cancellation/completion, and settlement evidence remains pending while the run is active.

### Disk safety and bounded assignment closeout

The repeated host exhaustion was traced to a T3 Code abort core staged by `systemd-coredump`, which could consume roughly 32 GB before normal TreeSeed cleanup had any opportunity to run. The user service manager now applies a zero core-file limit, and both Agent manager and runner Compose services declare `core: 0`; all four live Agent/platform-operation containers report the limit. SDK Docker reconciliation performs host headroom and reserve checks before builds, bounds cache retention, and removes only managed stale build material. Provider manager and runner admission also fail closed when host disk capacity is exhausted. Role-specific image preparation no longer duplicates the combined manager/runner source closure.

The first deliberately short four-agent workday then exposed a returned assignment being leased again inside its immutable closeout window. API lease admission now evaluates the assignment hard deadline, requires the configured closeout reserve (with a 30-second floor), caps lease expiry to that hard deadline, and records `assignment_hard_deadline_closeout_window` when a candidate is withheld. Live assignment `assignment_-iO73tqw6JI5J6fxCkexzcyjzz489kw3` executed once, returned once, remained at attempt count one, and was not leased again. API and Agent builds, focused lifecycle tests, package architecture checks, and 332/332 guarantee validation pass. The host remains stable at 55 GB free with 8.029 GB of bounded Docker build cache after repeated reconciliation and scene runs.

### Non-empty Atlas and forensic controls acceptance

The Agent Lab workspace-focus scene had previously passed while the isolated visual-audit team contained no project agent class, workday run, or event. The API-owned acceptance seed now creates its active Visual Audit Agent through `ProjectAgentClassService`, a safe queued capacity workday run, and 27 idempotent durable forensic events. The scene declares its visual-audit fixture role explicitly, so it refreshes those canonical API fixtures instead of trusting stale database state. Package regression coverage verifies the class, handler definition, queued run, and durable event rows.

Authenticated browser run `20260813t151928371z` passed all 17 steps with no console errors, network errors, warnings, or blockers. It proves a visible Atlas agent node, 27-event bounded window, page 1/page 2 navigation, text filtering and reset, compact/expanded vitals, focused Atlas, Shrink, Escape ordering, and browser-Back restoration. The viewport evidence is under `.treeseed/scenes/runs/agent-lab-workspace-focus/20260813T151928371Z-20260813t151928371z/`; full-page captures remain diagnostic only. Current scene output occupies tens of megabytes, not gigabytes, and disk usage did not grow during the repeated acceptance runs.

